# 18장 클래스 2 (상속과 메서드) — 연습문제

---

## 📝 객관식 문제

---

### 🟢 초급

**문제 1.** 파이썬에서 상속 문법으로 올바른 것은?

① `class Child extends Parent:`  
② `class Child(Parent):`  
③ `class Child inherits Parent:`  
④ `class Child <- Parent:`  

---

**문제 2.** 상속에 대한 설명으로 올바른 것은?

① 자식 클래스의 속성을 부모 클래스가 물려받는다  
② **부모 클래스의 속성과 메서드를 자식 클래스가 물려받는다**  
③ 상속받으면 부모 클래스가 삭제된다  
④ 하나의 부모 클래스에서 하나의 자식만 만들 수 있다  

---

**문제 3.** 다음 코드의 실행 결과는?

```python
class Animal:
    def eat(self):
        print("먹기")

class Dog(Animal):
    def bark(self):
        print("멍멍!")

dog = Dog()
dog.eat()
```

① 오류 발생  
② `먹기`  
③ `멍멍!`  
④ `먹기`와 `멍멍!` 모두 출력  

---

**문제 4.** 부모 클래스의 메서드를 호출할 때 사용하는 함수는?

① `parent()`  
② `base()`  
③ `super()`  
④ `inherit()`  

---

**문제 5.** 다음 코드의 실행 결과는?

```python
class Animal:
    def speak(self):
        print("...")

class Dog(Animal):
    def speak(self):
        print("멍멍!")

dog = Dog()
dog.speak()
```

① `...`  
② `멍멍!`  
③ `...`과 `멍멍!` 모두 출력  
④ 오류 발생  

---

**문제 6.** 메서드 오버라이딩(overriding)이란?

① 부모 메서드를 삭제하는 것  
② **자식 클래스에서 부모 메서드를 재정의하는 것**  
③ 새로운 메서드를 추가하는 것  
④ 메서드를 복사하는 것  

---

**문제 7.** 다음 코드의 실행 결과는?

```python
class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed

dog = Dog("멍멍이", "진돗개")
print(f"{dog.name} ({dog.breed})")
```

① 오류 발생  
② `멍멍이 (진돗개)`  
③ `Dog (진돗개)`  
④ `멍멍이`  

---

### 🟡 중급

**문제 8.** `isinstance(dog, Animal)`의 결과는? (`dog = Dog()`이고, `Dog`는 `Animal`을 상속받았을 때)

① `True`  
② `False`  
③ `None`  
④ 오류 발생  

---

**문제 9.** 다음 코드의 실행 결과는?

```python
class Shape:
    def area(self):
        return 0

class Rectangle(Shape):
    def __init__(self, width, height):
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height

r = Rectangle(5, 3)
print(r.area())
```

① `0`  
② `15`  
③ `8`  
④ 오류 발생  

---

**문제 10.** 다음 코드에서 `super().describe()`의 역할은?

```python
class Shape:
    def __init__(self, color):
        self.color = color
    def describe(self):
        print(f"색상: {self.color}")

class Circle(Shape):
    def __init__(self, color, radius):
        super().__init__(color)
        self.radius = radius
    def describe(self):
        super().describe()
        print(f"반지름: {self.radius}")
```

① `Circle`의 `describe()`를 호출  
② **`Shape`의 `describe()`를 호출**  
③ 오류 발생  
④ 아무 동작도 하지 않음  

---

**문제 11.** 다음 코드의 실행 결과는?

```python
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary
    
    def get_salary(self):
        return self.salary

class Manager(Employee):
    def __init__(self, name, salary, bonus):
        super().__init__(name, salary)
        self.bonus = bonus
    
    def get_salary(self):
        return self.salary + self.bonus

emp = Employee("김직원", 3000000)
mgr = Manager("박매니저", 4000000, 1000000)
print(emp.get_salary())
print(mgr.get_salary())
```

① `3000000`과 `4000000`  
② `3000000`과 `5000000`  
③ `4000000`과 `5000000`  
④ 오류 발생  

---

**문제 12.** `issubclass(Dog, Animal)`과 `issubclass(Animal, Dog)`의 결과는? (`Dog`는 `Animal`을 상속받았을 때)

① `True`, `True`  
② `True`, `False`  
③ `False`, `True`  
④ `False`, `False`  

---

### 🔴 고급

**문제 13.** 다음 코드의 실행 결과는?

```python
class A:
    def greet(self):
        return "안녕 A"

class B(A):
    def greet(self):
        return "안녕 B"

class C(B):
    pass

c = C()
print(c.greet())
```

① `안녕 A`  
② `안녕 B`  
③ 오류 발생  
④ `None`  

---

**문제 14.** 다음 코드의 실행 결과는?

```python
class Vehicle:
    def __init__(self, brand):
        self.brand = brand
        self.speed = 0
    
    def accelerate(self, amount):
        self.speed += amount

class Car(Vehicle):
    def __init__(self, brand, fuel):
        super().__init__(brand)
        self.fuel = fuel
    
    def accelerate(self, amount):
        if self.fuel > 0:
            super().accelerate(amount)
            self.fuel -= amount * 0.1

car = Car("현대", 50)
car.accelerate(100)
car.accelerate(100)
print(f"속도: {car.speed}, 연료: {car.fuel}")
```

① `속도: 200, 연료: 50`  
② `속도: 200, 연료: 30`  
③ `속도: 100, 연료: 40`  
④ `속도: 200, 연료: 30.0`  

---

**문제 15.** 다음 코드의 실행 결과는?

```python
class Animal:
    count = 0
    def __init__(self, name):
        self.name = name
        Animal.count += 1

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed

class Cat(Animal):
    def __init__(self, name, color):
        super().__init__(name)
        self.color = color

d1 = Dog("멍멍이", "진돗개")
d2 = Dog("바둑이", "시바견")
c1 = Cat("나비", "흰색")

print(Animal.count)
print(isinstance(d1, Animal))
print(isinstance(c1, Dog))
```

① `3`, `True`, `True`  
② `3`, `True`, `False`  
③ `2`, `True`, `False`  
④ `3`, `False`, `False`  

---

## 📝 주관식 문제

---

### 🟢 초급

**문제 16.** 상속의 개념을 실생활 예시를 들어 설명하시오. 부모 클래스와 자식 클래스가 각각 무엇인지 서술하시오.

---

**문제 17.** 다음 코드의 실행 결과를 쓰시오.

```python
class Animal:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def eat(self):
        print(f"{self.name}이(가) 먹습니다.")
    
    def sleep(self):
        print(f"{self.name}이(가) 잡니다.")

class Dog(Animal):
    def bark(self):
        print(f"{self.name}: 멍멍!")

class Cat(Animal):
    def meow(self):
        print(f"{self.name}: 야옹~")

dog = Dog("멍멍이", 3)
cat = Cat("나비", 2)

dog.eat()
dog.bark()
cat.eat()
cat.meow()
print(f"{dog.name}: {dog.age}살")
```

---

**문제 18.** 메서드 오버라이딩이 필요한 이유를 설명하시오. 부모의 `speak()` 메서드를 자식에서 오버라이딩하는 예시를 들어 서술하시오.

---

### 🟡 중급

**문제 19.** 다음 코드의 실행 결과를 쓰고, `super().__init__()`이 하는 역할을 설명하시오.

```python
class Shape:
    def __init__(self, color):
        self.color = color
    
    def describe(self):
        print(f"색상: {self.color}")

class Rectangle(Shape):
    def __init__(self, color, width, height):
        super().__init__(color)
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height
    
    def describe(self):
        super().describe()
        print(f"크기: {self.width} x {self.height}")
        print(f"넓이: {self.area()}")

rect = Rectangle("빨강", 10, 5)
rect.describe()
```

---

**문제 20.** `isinstance()`와 `issubclass()`의 차이를 설명하고, 다음 코드의 실행 결과를 쓰시오.

```python
class Animal:
    pass

class Dog(Animal):
    pass

class Cat(Animal):
    pass

dog = Dog()

print(isinstance(dog, Dog))
print(isinstance(dog, Animal))
print(isinstance(dog, Cat))
print(issubclass(Dog, Animal))
print(issubclass(Animal, Dog))
```

---

### 🔴 고급

**문제 21.** 다음 코드의 실행 결과를 쓰고, 각 클래스의 `get_salary()` 오버라이딩이 어떻게 다른 결과를 만드는지 설명하시오.

```python
class Employee:
    def __init__(self, name, base_salary):
        self.name = name
        self.base_salary = base_salary
    
    def get_salary(self):
        return self.base_salary

class Manager(Employee):
    def __init__(self, name, base_salary, team_size):
        super().__init__(name, base_salary)
        self.team_size = team_size
    
    def get_salary(self):
        return self.base_salary + (self.team_size * 100000)

class Developer(Employee):
    def __init__(self, name, base_salary, language):
        super().__init__(name, base_salary)
        self.language = language
    
    def get_salary(self):
        return self.base_salary + 500000

employees = [
    Employee("김직원", 3000000),
    Manager("박매니저", 4000000, 5),
    Developer("이개발", 3500000, "Python")
]

for emp in employees:
    print(f"{emp.name}: {emp.get_salary():,}원")
```

---

## 💻 실습형 문제

---

### 🟢 초급

**문제 22.** 다음 요구사항에 맞는 클래스를 작성하시오.

> 부모 클래스 `Animal`과 자식 클래스 `Dog`, `Cat`을 만드시오.
> - `Animal`: `__init__(name, age)`, `eat()` — "이름이(가) 먹습니다" 출력
> - `Dog(Animal)`: `bark()` — "이름: 멍멍!" 출력
> - `Cat(Animal)`: `meow()` — "이름: 야옹~" 출력

출력 예시:

```
멍멍이이(가) 먹습니다.
멍멍이: 멍멍!
나비이(가) 먹습니다.
나비: 야옹~
```

---

**문제 23.** 다음 요구사항에 맞는 클래스를 작성하시오.

> 부모 클래스 `Shape`와 자식 클래스 `Rectangle`, `Circle`을 만드시오.
> - `Shape`: `__init__(color)`, `describe()` — "색상: ..." 출력
> - `Rectangle(Shape)`: `__init__(color, width, height)`, `area()` — 넓이 반환
> - `Circle(Shape)`: `__init__(color, radius)`, `area()` — 넓이 반환 (`3.14 * r²`)
> - 두 자식 클래스 모두 `describe()`를 오버라이딩하여 색상 + 넓이 출력

출력 예시:

```
색상: 빨강
넓이: 50

색상: 파랑
넓이: 153.86
```

---

### 🟡 중급

**문제 24.** 다음 요구사항에 맞는 은행 계좌 상속 시스템을 작성하시오.

> - `BankAccount`: 기본 입출금 기능 (`deposit`, `withdraw`, `show_info`)
> - `SavingsAccount(BankAccount)`: 이자 계산 기능 추가 (`add_interest`)
>   - 생성 시 이율(`interest_rate`) 설정 (기본값 0.03)
>   - `add_interest()`: 잔액 × 이율만큼 이자 추가
> - `CheckingAccount(BankAccount)`: 마이너스 출금 기능
>   - 생성 시 한도(`overdraft_limit`) 설정 (기본값 500000)
>   - `withdraw()` 오버라이딩: 잔액 + 한도까지 출금 가능

출력 예시:

```
=== 적금 계좌 ===
✓ 50,000원 입금 완료 (잔액: 1,050,000원)
✓ 이자 31,500원 추가 (이율: 3.0%)
잔액: 1,081,500원

=== 당좌 계좌 ===
✓ 300,000원 출금 완료 (잔액: -200,000원)
출금 가능: 300,000원
```

---

**문제 25.** 다음 요구사항에 맞는 학생 상속 시스템을 작성하시오.

> - `Student`: 기본 학생 (`name`, `student_id`, `scores` 리스트)
>   - `add_score(score)`, `get_average()`, `show_info()`
> - `Undergraduate(Student)`: 학부생 — `major`(전공) 속성 추가
>   - `show_info()` 오버라이딩: 전공 추가 출력
> - `Graduate(Student)`: 대학원생 — `advisor`(지도교수), `thesis`(논문 제목) 속성 추가
>   - `show_info()` 오버라이딩: 지도교수, 논문 추가 출력
>   - `get_average()` 오버라이딩: 평균 80점 미만이면 "경고" 표시

출력 예시:

```
=== 학부생 ===
이름: 김철수 (학번: U001)
전공: 컴퓨터공학
성적: [85, 90, 78]
평균: 84.3점

=== 대학원생 ===
이름: 박영희 (학번: G001)
지도교수: 조정현
논문: 머신러닝 연구
성적: [92, 88, 95]
평균: 91.7점
```

---

### 🔴 고급

**문제 26.** 다음 요구사항에 맞는 온라인 쇼핑몰 시스템을 작성하시오.

> - `Product`: 기본 상품 클래스
>   - `__init__(name, price)`, `get_price()`, `info()`
>   - 클래스 변수 `total_products`로 전체 상품 수 관리
> - `DiscountProduct(Product)`: 할인 상품
>   - `discount_rate`(할인율) 속성 추가
>   - `get_price()` 오버라이딩: 할인 적용 가격 반환
>   - `info()` 오버라이딩: 원가, 할인율, 할인가 출력
> - `BundleProduct(Product)`: 묶음 상품
>   - `items` 리스트 속성 추가 (Product 객체들의 리스트)
>   - `add_item(product)`: 묶음에 상품 추가
>   - `get_price()` 오버라이딩: 묶음 내 상품 합계에서 10% 할인
>   - `info()` 오버라이딩: 구성 상품과 묶음 가격 출력

출력 예시:

```
=== 일반 상품 ===
[상품] 키보드 - 50,000원

=== 할인 상품 ===
[할인] 마우스 - 원가: 30,000원 → 할인가: 24,000원 (20% 할인)

=== 묶음 상품 ===
[묶음] PC 세트
  - 키보드: 50,000원
  - 마우스(할인): 24,000원
  - 모니터: 300,000원
  합계: 374,000원 → 묶음가: 336,600원 (10% 추가 할인)

총 상품 수: 4개
```

---
---

# 🔑 정답 및 해설

---

## 📝 객관식 정답

---

### 🟢 초급

**문제 1. 정답: ② `class Child(Parent):`**

파이썬에서는 `class 자식(부모):` 형태로 상속합니다. `extends`는 Java, `inherits`는 VB.NET의 문법입니다.

---

**문제 2. 정답: ② 부모 클래스의 속성과 메서드를 자식 클래스가 물려받는다**

상속은 부모→자식 방향입니다. 부모의 모든 속성과 메서드를 자식이 물려받아 사용할 수 있습니다.

---

**문제 3. 정답: ② `먹기`**

`Dog`는 `Animal`을 상속받았으므로 부모의 `eat()` 메서드를 사용할 수 있습니다. `dog.eat()`은 `"먹기"`를 출력합니다.

---

**문제 4. 정답: ③ `super()`**

`super()`는 부모 클래스를 참조하여 부모의 메서드를 호출할 때 사용합니다.

---

**문제 5. 정답: ② `멍멍!`**

`Dog`에서 `speak()`를 오버라이딩했으므로, `dog.speak()`는 자식의 메서드가 우선 실행되어 `"멍멍!"`이 출력됩니다.

---

**문제 6. 정답: ② 자식 클래스에서 부모 메서드를 재정의하는 것**

오버라이딩은 부모의 메서드를 자식에서 같은 이름으로 다시 정의하여, 자식만의 동작으로 바꾸는 것입니다.

---

**문제 7. 정답: ② `멍멍이 (진돗개)`**

`super().__init__(name)`으로 부모의 `__init__`을 호출하여 `self.name = "멍멍이"`를 설정하고, `self.breed = "진돗개"`도 설정됩니다.

---

### 🟡 중급

**문제 8. 정답: ① `True`**

`Dog`는 `Animal`을 상속받았으므로 `Dog` 객체는 `Animal`의 인스턴스이기도 합니다. `isinstance(dog, Animal)`은 `True`입니다.

---

**문제 9. 정답: ② `15`**

`Rectangle`에서 `area()`를 오버라이딩하여 `width * height`를 반환합니다. `5 * 3 = 15`

---

**문제 10. 정답: ② `Shape`의 `describe()`를 호출**

`super().describe()`는 부모 클래스인 `Shape`의 `describe()` 메서드를 호출합니다. 이를 통해 부모의 기능을 유지하면서 자식만의 추가 출력을 할 수 있습니다.

---

**문제 11. 정답: ② `3000000`과 `5000000`**

- `Employee`의 `get_salary()` → `base_salary` = 3,000,000
- `Manager`의 `get_salary()` → `salary + bonus` = 4,000,000 + 1,000,000 = 5,000,000

---

**문제 12. 정답: ② `True`, `False`**

- `issubclass(Dog, Animal)` → `Dog`는 `Animal`의 자식 → `True`
- `issubclass(Animal, Dog)` → `Animal`은 `Dog`의 자식이 아님 → `False`

---

### 🔴 고급

**문제 13. 정답: ② `안녕 B`**

`C`는 `B`를 상속받고, `greet()`를 오버라이딩하지 않았습니다. 파이썬은 `C` → `B` → `A` 순서로 메서드를 찾으므로 `B`의 `greet()`가 실행됩니다.

---

**문제 14. 정답: ④ `속도: 200, 연료: 30.0`**

- 초기: `speed=0`, `fuel=50`
- 1차 `accelerate(100)`: `fuel>0`이므로 → `speed=100`, `fuel = 50 - 10.0 = 40.0`
- 2차 `accelerate(100)`: `fuel>0`이므로 → `speed=200`, `fuel = 40.0 - 10.0 = 30.0`
- `100 * 0.1 = 10.0`(float)이므로 fuel도 float가 됩니다.

---

**문제 15. 정답: ② `3`, `True`, `False`**

- `Animal.count`: Dog 2마리 + Cat 1마리 = 3 (모두 `super().__init__`에서 `Animal.count += 1` 호출)
- `isinstance(d1, Animal)` → Dog는 Animal 상속 → `True`
- `isinstance(c1, Dog)` → Cat은 Dog가 아님 → `False`

---

## 📝 주관식 정답

---

### 🟢 초급

**문제 16. 모범답안:**

상속은 **부모 클래스의 속성과 메서드를 자식 클래스가 물려받는 것**입니다.

실생활 예시: 동물 분류 체계
- **부모 클래스**: `동물` — 모든 동물이 공통으로 가지는 특성(이름, 나이)과 행동(먹기, 자기)을 정의
- **자식 클래스**: `개`, `고양이` — 동물의 공통 특성을 물려받으면서, 자신만의 고유한 행동(짖기, 야옹거리기)을 추가

상속의 장점:
- 코드 재사용: 공통 코드를 부모에 한 번만 작성
- 유지보수 용이: 공통 기능 수정 시 부모만 수정하면 됨
- 확장성: 기존 코드를 건드리지 않고 새로운 자식 클래스 추가 가능

---

**문제 17. 모범답안:**

```
멍멍이이(가) 먹습니다.
멍멍이: 멍멍!
나비이(가) 먹습니다.
나비: 야옹~
멍멍이: 3살
```

- `Dog`와 `Cat`은 `Animal`을 상속받아 `eat()` 메서드를 사용할 수 있습니다.
- `dog.eat()` → 부모의 `eat()` 실행 → `"멍멍이이(가) 먹습니다."`
- `dog.bark()` → 자식만의 메서드 → `"멍멍이: 멍멍!"`
- `cat.eat()` → 부모의 `eat()` 실행 → `"나비이(가) 먹습니다."`
- `cat.meow()` → 자식만의 메서드 → `"나비: 야옹~"`
- `dog.name`, `dog.age`는 부모의 `__init__`에서 설정된 속성

---

**문제 18. 모범답안:**

메서드 오버라이딩은 부모의 **기본 동작이 자식에게 맞지 않을 때**, 자식이 같은 이름으로 메서드를 재정의하여 자신만의 동작을 구현하는 것입니다.

예시: `Animal`의 `speak()` 메서드
- `Animal.speak()` → 기본: `"소리를 냅니다"`
- `Dog.speak()` → 오버라이딩: `"멍멍!"`
- `Cat.speak()` → 오버라이딩: `"야옹~"`

같은 `speak()`를 호출해도 각 객체의 클래스에서 오버라이딩한 메서드가 실행되어 다른 결과를 만듭니다. 이를 통해 통일된 인터페이스(`speak()`)로 다양한 동작을 수행할 수 있습니다.

---

### 🟡 중급

**문제 19. 모범답안:**

**실행 결과:**

```
색상: 빨강
크기: 10 x 5
넓이: 50
```

`super().__init__(color)`의 역할:
- `Rectangle`의 `__init__`에서 부모 `Shape`의 `__init__`을 호출합니다.
- 이를 통해 `self.color = color`가 설정됩니다.
- `super().__init__`을 호출하지 않으면 `self.color` 속성이 생성되지 않아, `describe()`에서 오류가 발생합니다.

`super().describe()`의 역할:
- `Rectangle`의 `describe()`에서 먼저 부모의 `describe()`를 호출하여 `"색상: 빨강"`을 출력합니다.
- 그 다음 자식만의 추가 정보(크기, 넓이)를 출력합니다.
- 부모의 기능을 유지하면서 확장하는 패턴입니다.

---

**문제 20. 모범답안:**

**실행 결과:**

```
True
True
False
True
False
```

**`isinstance(객체, 클래스)`**: 객체가 해당 클래스(또는 그 부모 클래스)의 인스턴스인지 확인합니다.
- `isinstance(dog, Dog)` → `True` (dog은 Dog)
- `isinstance(dog, Animal)` → `True` (Dog는 Animal을 상속, 부모도 True)
- `isinstance(dog, Cat)` → `False` (dog은 Cat이 아님)

**`issubclass(클래스A, 클래스B)`**: 클래스A가 클래스B의 자식(하위) 클래스인지 확인합니다.
- `issubclass(Dog, Animal)` → `True` (Dog는 Animal의 자식)
- `issubclass(Animal, Dog)` → `False` (Animal은 Dog의 자식이 아님)

---

### 🔴 고급

**문제 21. 모범답안:**

**실행 결과:**

```
김직원: 3,000,000원
박매니저: 4,500,000원
이개발: 4,000,000원
```

- **`Employee.get_salary()`**: `base_salary`만 반환 → 3,000,000원
- **`Manager.get_salary()`**: `base_salary + (team_size × 100,000)` = 4,000,000 + (5 × 100,000) = 4,500,000원
- **`Developer.get_salary()`**: `base_salary + 500,000` = 3,500,000 + 500,000 = 4,000,000원

`for emp in employees`에서 같은 `get_salary()`를 호출하지만, 각 객체의 **실제 클래스에 따라** 오버라이딩된 메서드가 실행됩니다. 이것이 오버라이딩의 핵심입니다.

---

## 💻 실습형 정답

---

### 🟢 초급

**문제 22. 모범답안:**

```python
class Animal:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def eat(self):
        print(f"{self.name}이(가) 먹습니다.")

class Dog(Animal):
    def bark(self):
        print(f"{self.name}: 멍멍!")

class Cat(Animal):
    def meow(self):
        print(f"{self.name}: 야옹~")

# 테스트
dog = Dog("멍멍이", 3)
cat = Cat("나비", 2)
dog.eat()
dog.bark()
cat.eat()
cat.meow()
```

핵심: `Dog`와 `Cat`은 `Animal`을 상속받아 `__init__`과 `eat()`을 그대로 사용하면서, 자신만의 메서드를 추가합니다.

---

**문제 23. 모범답안:**

```python
class Shape:
    def __init__(self, color):
        self.color = color
    
    def describe(self):
        print(f"색상: {self.color}")

class Rectangle(Shape):
    def __init__(self, color, width, height):
        super().__init__(color)
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height
    
    def describe(self):
        super().describe()
        print(f"넓이: {self.area()}")

class Circle(Shape):
    def __init__(self, color, radius):
        super().__init__(color)
        self.radius = radius
    
    def area(self):
        return 3.14 * self.radius ** 2
    
    def describe(self):
        super().describe()
        print(f"넓이: {self.area():.2f}")

# 테스트
rect = Rectangle("빨강", 10, 5)
rect.describe()
print()
circle = Circle("파랑", 7)
circle.describe()
```

핵심: `super().__init__(color)`로 부모 초기화, `super().describe()`로 부모 메서드 호출 후 추가 출력합니다.

---

### 🟡 중급

**문제 24. 모범답안:**

```python
class BankAccount:
    def __init__(self, owner, balance=0):
        self.owner = owner
        self.balance = balance
    
    def deposit(self, amount):
        if amount > 0:
            self.balance += amount
            print(f"✓ {amount:,}원 입금 완료 (잔액: {self.balance:,}원)")
            return True
        return False
    
    def withdraw(self, amount):
        if amount > self.balance:
            print(f"❌ 잔액 부족 (잔액: {self.balance:,}원)")
            return False
        self.balance -= amount
        print(f"✓ {amount:,}원 출금 완료 (잔액: {self.balance:,}원)")
        return True
    
    def show_info(self):
        print(f"예금주: {self.owner} | 잔액: {self.balance:,}원")

class SavingsAccount(BankAccount):
    def __init__(self, owner, balance=0, interest_rate=0.03):
        super().__init__(owner, balance)
        self.interest_rate = interest_rate
    
    def add_interest(self):
        interest = int(self.balance * self.interest_rate)
        self.balance += interest
        print(f"✓ 이자 {interest:,}원 추가 (이율: {self.interest_rate*100}%)")
        print(f"잔액: {self.balance:,}원")

class CheckingAccount(BankAccount):
    def __init__(self, owner, balance=0, overdraft_limit=500000):
        super().__init__(owner, balance)
        self.overdraft_limit = overdraft_limit
    
    def withdraw(self, amount):
        max_withdraw = self.balance + self.overdraft_limit
        if amount > max_withdraw:
            print(f"❌ 한도 초과 (출금 가능: {max_withdraw:,}원)")
            return False
        self.balance -= amount
        print(f"✓ {amount:,}원 출금 완료 (잔액: {self.balance:,}원)")
        available = self.balance + self.overdraft_limit
        print(f"출금 가능: {available:,}원")
        return True

# 테스트
print("=== 적금 계좌 ===")
savings = SavingsAccount("박영희", 1000000, 0.03)
savings.deposit(50000)
savings.add_interest()

print("\n=== 당좌 계좌 ===")
checking = CheckingAccount("이민수", 100000, 500000)
checking.withdraw(300000)
```

핵심: `SavingsAccount`는 이자 계산 메서드를 추가하고, `CheckingAccount`는 `withdraw()`를 오버라이딩하여 마이너스 출금을 허용합니다.

---

**문제 25. 모범답안:**

```python
class Student:
    def __init__(self, name, student_id):
        self.name = name
        self.student_id = student_id
        self.scores = []
    
    def add_score(self, score):
        self.scores.append(score)
    
    def get_average(self):
        if len(self.scores) == 0:
            return 0
        return sum(self.scores) / len(self.scores)
    
    def show_info(self):
        print(f"이름: {self.name} (학번: {self.student_id})")
        print(f"성적: {self.scores}")
        print(f"평균: {self.get_average():.1f}점")

class Undergraduate(Student):
    def __init__(self, name, student_id, major):
        super().__init__(name, student_id)
        self.major = major
    
    def show_info(self):
        print(f"이름: {self.name} (학번: {self.student_id})")
        print(f"전공: {self.major}")
        print(f"성적: {self.scores}")
        print(f"평균: {self.get_average():.1f}점")

class Graduate(Student):
    def __init__(self, name, student_id, advisor, thesis):
        super().__init__(name, student_id)
        self.advisor = advisor
        self.thesis = thesis
    
    def get_average(self):
        avg = super().get_average()
        if avg > 0 and avg < 80:
            print("⚠️ 경고: 평균 80점 미만입니다!")
        return avg
    
    def show_info(self):
        print(f"이름: {self.name} (학번: {self.student_id})")
        print(f"지도교수: {self.advisor}")
        print(f"논문: {self.thesis}")
        print(f"성적: {self.scores}")
        print(f"평균: {self.get_average():.1f}점")

# 테스트
print("=== 학부생 ===")
undergrad = Undergraduate("김철수", "U001", "컴퓨터공학")
undergrad.add_score(85)
undergrad.add_score(90)
undergrad.add_score(78)
undergrad.show_info()

print("\n=== 대학원생 ===")
grad = Graduate("박영희", "G001", "조정현", "머신러닝 연구")
grad.add_score(92)
grad.add_score(88)
grad.add_score(95)
grad.show_info()
```

핵심: `Undergraduate`와 `Graduate`가 `show_info()`를 오버라이딩합니다. `Graduate`는 `get_average()`도 오버라이딩하여 80점 미만 경고를 추가합니다.

---

### 🔴 고급

**문제 26. 모범답안:**

```python
class Product:
    total_products = 0
    
    def __init__(self, name, price):
        self.name = name
        self.price = price
        Product.total_products += 1
    
    def get_price(self):
        return self.price
    
    def info(self):
        print(f"[상품] {self.name} - {self.get_price():,}원")

class DiscountProduct(Product):
    def __init__(self, name, price, discount_rate):
        super().__init__(name, price)
        self.discount_rate = discount_rate
    
    def get_price(self):
        return int(self.price * (1 - self.discount_rate))
    
    def info(self):
        original = self.price
        discounted = self.get_price()
        rate = int(self.discount_rate * 100)
        print(f"[할인] {self.name} - 원가: {original:,}원 → 할인가: {discounted:,}원 ({rate}% 할인)")

class BundleProduct(Product):
    def __init__(self, name):
        super().__init__(name, 0)
        self.items = []
    
    def add_item(self, product):
        self.items.append(product)
    
    def get_price(self):
        total = 0
        for item in self.items:
            total += item.get_price()
        return int(total * 0.9)
    
    def info(self):
        total = sum(item.get_price() for item in self.items)
        bundle_price = self.get_price()
        
        print(f"[묶음] {self.name}")
        for item in self.items:
            print(f"  - {item.name}: {item.get_price():,}원")
        print(f"  합계: {total:,}원 → 묶음가: {bundle_price:,}원 (10% 추가 할인)")

# 테스트
print("=== 일반 상품 ===")
keyboard = Product("키보드", 50000)
keyboard.info()

print("\n=== 할인 상품 ===")
mouse = DiscountProduct("마우스", 30000, 0.2)
mouse.info()

print("\n=== 묶음 상품 ===")
monitor = Product("모니터", 300000)
bundle = BundleProduct("PC 세트")
bundle.add_item(keyboard)
bundle.add_item(mouse)
bundle.add_item(monitor)
bundle.info()

print(f"\n총 상품 수: {Product.total_products}개")
```

핵심 포인트:
- `DiscountProduct`는 `get_price()`를 오버라이딩하여 할인 적용 가격을 반환합니다.
- `BundleProduct`는 내부 `items` 리스트에 `Product` 객체들을 저장하고, 각 상품의 `get_price()`를 호출하여 합계를 계산합니다. `DiscountProduct`가 포함되면 할인 가격이 자동으로 적용됩니다.
- 클래스 변수 `total_products`는 모든 `Product` (자식 포함) 생성 시 증가합니다.

---

조정현 교수 (peterchokr@gmail.com)  
영남이공대학교
