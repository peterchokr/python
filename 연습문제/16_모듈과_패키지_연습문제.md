# 16장 모듈과 패키지 — 연습문제

---

## 📝 객관식 문제

---

### 🟢 초급

**문제 1.** 파이썬에서 모듈을 가져오는 키워드는?

① `include`  
② `require`  
③ `import`  
④ `load`  

---

**문제 2.** 모듈(Module)에 대한 설명으로 옳은 것은?

① 파이썬에서만 사용할 수 있는 특수 파일이다  
② 관련된 함수, 변수 등을 하나의 `.py` 파일에 모아둔 것이다  
③ 반드시 클래스를 포함해야 한다  
④ 한 번 import하면 수정할 수 없다  

---

**문제 3.** 다음 코드의 실행 결과는?

```python
import math
print(math.sqrt(25))
```

① `25`  
② `5`  
③ `5.0`  
④ 오류 발생  

---

**문제 4.** `from math import pi`를 실행한 후, `pi` 값을 사용하는 올바른 방법은?

① `math.pi`  
② `pi`  
③ `import.pi`  
④ `from.pi`  

---

**문제 5.** 다음 중 올바른 import 문법은?

① `import math.pi`  
② `from math import pi`  
③ `import pi from math`  
④ `get pi from math`  

---

**문제 6.** `random` 모듈의 `randint(1, 6)` 함수가 반환할 수 있는 값의 범위는?

① 1 이상 5 이하  
② 1 이상 6 미만  
③ 1 이상 6 이하  
④ 0 이상 6 미만  

---

**문제 7.** 다음 코드의 실행 결과는?

```python
import math as m
print(m.factorial(4))
```

① `4`  
② `10`  
③ `24`  
④ 오류 발생  

---

### 🟡 중급

**문제 8.** `from math import sqrt as s` 실행 후, `s(16)`의 결과는?

① 오류 발생  
② `s(16)`  
③ `4.0`  
④ `16`  

---

**문제 9.** `__name__ == "__main__"`이 `True`가 되는 경우는?

① 모듈이 import될 때  
② 모듈이 **직접 실행**될 때  
③ 모듈에 오류가 있을 때  
④ 모듈이 처음 만들어질 때  

---

**문제 10.** 다음 코드의 결과로 옳은 것은?

```python
from datetime import datetime
now = datetime.now()
print(type(now).__name__)
```

① `str`  
② `datetime`  
③ `date`  
④ `time`  

---

**문제 11.** 패키지(Package)를 나타내기 위해 폴더 안에 반드시 있어야 하는 파일은?

① `__main__.py`  
② `__init__.py`  
③ `__package__.py`  
④ `index.py`  

---

**문제 12.** `random.choice(["A", "B", "C"])`의 결과로 가능한 것은?

① `["A", "B", "C"]`  
② `"A"`, `"B"`, `"C"` 중 하나  
③ 항상 `"A"`  
④ `0`, `1`, `2` 중 하나  

---

### 🔴 고급

**문제 13.** 다음 코드에서 `calculator.py`를 import할 때 테스트 코드가 **실행되지 않는** 이유는?

```python
# calculator.py
def add(a, b):
    return a + b

if __name__ == "__main__":
    print(f"테스트: {add(1, 2)}")
```

① `if` 문의 조건이 항상 `False`이기 때문  
② import 시 `__name__`이 `"__main__"`이 아닌 **모듈 이름**이 되기 때문  
③ `if` 문이 함수 밖에 있기 때문  
④ `print` 함수가 import 시 비활성화되기 때문  

---

**문제 14.** 다음 코드의 실행 결과는?

```python
import random
random.seed(42)
print(random.randint(1, 10))
random.seed(42)
print(random.randint(1, 10))
```

① 서로 다른 두 숫자  
② **같은 숫자 두 번** 출력  
③ 오류 발생  
④ 항상 42 출력  

---

**문제 15.** 외부 패키지를 설치하는 명령어로 올바른 것은?

① `python install requests`  
② `pip install requests`  
③ `import install requests`  
④ `download requests`  

---

## 📝 주관식 문제

---

### 🟢 초급

**문제 16.** 모듈을 사용하는 3가지 import 방법을 각각 예시와 함께 설명하시오.

```python
# 방법 1: ???
# 방법 2: ???
# 방법 3: ???
```

---

**문제 17.** 다음 코드의 실행 결과를 쓰시오.

```python
import math

print(math.ceil(3.2))
print(math.floor(3.8))
print(math.pow(2, 3))
```

---

**문제 18.** `random` 모듈의 다음 함수들의 차이점을 각각 설명하시오.

- `random.randint(1, 6)`
- `random.choice(리스트)`
- `random.shuffle(리스트)`
- `random.sample(리스트, 개수)`

---

### 🟡 중급

**문제 19.** `if __name__ == "__main__":`의 역할을 설명하시오. 직접 실행할 때와 import할 때 `__name__` 변수의 값이 어떻게 달라지는지 구체적으로 서술하시오.

---

**문제 20.** 다음 코드의 실행 결과를 쓰시오.

```python
from datetime import datetime, timedelta

base = datetime(2025, 1, 1)
after_30 = base + timedelta(days=30)
after_1week = base + timedelta(weeks=1)

print(base.strftime("%Y-%m-%d"))
print(after_30.strftime("%Y년 %m월 %d일"))
print(after_1week.strftime("%m/%d"))
print((after_30 - base).days)
```

---

### 🔴 고급

**문제 21.** 모듈과 패키지의 차이점을 설명하고, 다음 프로젝트 구조에서 `main.py`가 `calculator.py`의 `add` 함수를 사용하는 2가지 import 방법을 코드로 쓰시오.

```
my_project/
├── main.py
└── utils/
    ├── __init__.py
    └── calculator.py    # def add(a, b): return a + b
```

---

## 💻 실습형 문제

---

### 🟢 초급

**문제 22.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> `math` 모듈을 활용하여 사용자가 입력한 반지름으로 원의 넓이와 둘레를 계산하시오.
> - 원의 넓이: `math.pi * r ** 2`
> - 원의 둘레: `2 * math.pi * r`
> - 소수점 2자리까지 출력

출력 예시:

```
반지름을 입력하세요: 5
원의 넓이: 78.54
원의 둘레: 31.42
```

---

**문제 23.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> `random` 모듈을 활용하여 로또 번호 생성기를 만드시오.
> - 1~45 중 **중복 없이** 6개 번호 선택 (`random.sample()` 사용)
> - 번호를 **오름차순 정렬**하여 출력
> - 사용자가 원하는 세트 수만큼 생성

출력 예시:

```
생성할 세트 수: 3

[1세트] [3, 12, 18, 27, 35, 42]
[2세트] [5, 9, 21, 30, 38, 44]
[3세트] [1, 7, 15, 23, 31, 40]
```

---

### 🟡 중급

**문제 24.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> `datetime` 모듈을 활용하여 D-day 계산기를 만드시오.
> - 목표 날짜를 `YYYY-MM-DD` 형식으로 입력받음
> - 오늘부터 목표일까지 남은 일수 계산
> - 이미 지난 날짜면 "X일 전에 지났습니다" 출력
> - 잘못된 형식 입력 시 `ValueError` 예외 처리

출력 예시:

```
목표 날짜 (YYYY-MM-DD): 2025-12-25
오늘: 2025-02-12
목표: 2025-12-25
D-316 (316일 남았습니다)

목표 날짜 (YYYY-MM-DD): 2024-01-01
오늘: 2025-02-12
목표: 2024-01-01
D+408 (408일 전에 지났습니다)
```

---

**문제 25.** 다음 요구사항에 맞는 모듈과 메인 프로그램을 작성하시오.

> `string_utils.py` 모듈을 만들고, `main.py`에서 import하여 사용하시오.
>
> **string_utils.py** 에 포함할 함수:
> - `count_chars(text)`: 공백 제외 글자 수 반환
> - `count_words(text)`: 단어 수 반환
> - `find_longest(text)`: 가장 긴 단어 반환
> - `reverse_words(text)`: 단어 순서 뒤집기
>
> **main.py**: 모듈을 import하여 입력받은 문장을 분석

출력 예시:

```
문장을 입력하세요: Hello Python World

=== 문장 분석 결과 ===
글자 수 (공백 제외): 16
단어 수: 3
가장 긴 단어: Python
단어 뒤집기: World Python Hello
```

---

### 🔴 고급

**문제 26.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> `random`, `datetime` 모듈을 모두 활용한 퀴즈 게임 시스템을 만드시오.
>
> - 덧셈, 뺄셈, 곱셈 문제를 랜덤으로 5문제 출제
> - 숫자 범위: 1~50 랜덤
> - 정답/오답 판정 및 점수 계산
> - `datetime`으로 총 소요 시간 측정
> - 잘못된 입력(숫자가 아닌 값)은 `try-except`로 처리
> - 게임 종료 후 결과표 출력

출력 예시:

```
=== 수학 퀴즈 ===
총 5문제 | 시작 시간: 14:30:25

[1/5] 23 + 17 = ? 답: 40
✓ 정답!

[2/5] 45 - 12 = ? 답: 30
✗ 오답! (정답: 33)

[3/5] 7 * 8 = ? 답: abc
❌ 숫자를 입력해주세요! (오답 처리)

[4/5] 38 + 9 = ? 답: 47
✓ 정답!

[5/5] 15 * 3 = ? 답: 45
✓ 정답!

=== 결과 ===
점수: 3 / 5 (60.0%)
소요 시간: 25초
```

---
---

# 🔑 정답 및 해설

---

## 📝 객관식 정답

---

### 🟢 초급

**문제 1. 정답: ③ `import`**

파이썬에서는 `import` 키워드로 모듈을 가져옵니다. `include`는 C/C++, `require`는 JavaScript/Ruby에서 사용합니다.

---

**문제 2. 정답: ② 관련된 함수, 변수 등을 하나의 `.py` 파일에 모아둔 것이다**

모듈은 파이썬 파일(`.py`) 하나를 의미하며, 관련된 함수, 변수, 클래스 등을 담고 있습니다. 클래스가 반드시 필요하지 않으며, 수정도 가능합니다.

---

**문제 3. 정답: ③ `5.0`**

`math.sqrt(25)`는 25의 제곱근을 **실수(float)** 로 반환합니다. 정수 `5`가 아닌 `5.0`입니다.

---

**문제 4. 정답: ② `pi`**

`from math import pi`는 `pi`를 직접 가져오므로, 모듈 이름 없이 `pi`만으로 사용합니다. `math.pi`는 `import math`를 했을 때의 사용 방법입니다.

---

**문제 5. 정답: ② `from math import pi`**

`from 모듈 import 이름` 형식이 올바른 문법입니다. `import math.pi`는 잘못된 문법이며, `import pi from math`는 순서가 바뀌었습니다.

---

**문제 6. 정답: ③ 1 이상 6 이하**

`random.randint(a, b)`는 a 이상 b **이하**의 정수를 반환합니다. 즉 `randint(1, 6)`은 1, 2, 3, 4, 5, 6 중 하나입니다.

---

**문제 7. 정답: ③ `24`**

`import math as m`으로 math 모듈을 `m`이라는 별칭으로 사용합니다. `m.factorial(4) = 4! = 4 × 3 × 2 × 1 = 24`

---

### 🟡 중급

**문제 8. 정답: ③ `4.0`**

`from math import sqrt as s`로 `sqrt`를 `s`라는 별칭으로 가져왔으므로, `s(16) = sqrt(16) = 4.0`입니다.

---

**문제 9. 정답: ② 모듈이 직접 실행될 때**

파이썬 파일을 직접 실행하면 `__name__`이 `"__main__"`이 됩니다. 다른 파일에서 import하면 `__name__`은 모듈 이름(파일 이름)이 됩니다.

---

**문제 10. 정답: ② `datetime`**

`datetime.now()`는 `datetime` 객체를 반환합니다. `type(now).__name__`은 객체의 타입 이름을 문자열로 반환하므로 `"datetime"`입니다.

---

**문제 11. 정답: ② `__init__.py`**

`__init__.py` 파일이 있는 디렉토리를 파이썬이 패키지로 인식합니다. 이 파일은 비어있어도 됩니다.

---

**문제 12. 정답: ② `"A"`, `"B"`, `"C"` 중 하나**

`random.choice(리스트)`는 리스트에서 요소 **하나**를 랜덤으로 선택하여 반환합니다.

---

### 🔴 고급

**문제 13. 정답: ② import 시 `__name__`이 `"__main__"`이 아닌 모듈 이름이 되기 때문**

`calculator.py`를 import하면 `__name__`은 `"calculator"`가 됩니다. `"calculator" == "__main__"`은 `False`이므로 if 블록 안의 테스트 코드는 실행되지 않습니다.

---

**문제 14. 정답: ② 같은 숫자 두 번 출력**

`random.seed(42)`로 같은 시드를 설정하면 난수 생성 순서가 동일해집니다. 따라서 두 번 모두 같은 숫자가 출력됩니다.

---

**문제 15. 정답: ② `pip install requests`**

`pip`는 파이썬의 패키지 관리 도구입니다. `pip install 패키지이름`으로 외부 패키지를 설치합니다.

---

## 📝 주관식 정답

---

### 🟢 초급

**문제 16. 모범답안:**

**방법 1: 모듈 전체 가져오기 (`import`)**
```python
import math
print(math.sqrt(16))    # 모듈이름.함수이름()
print(math.pi)           # 모듈이름.변수이름
```
모듈 안의 모든 기능을 사용할 수 있지만, 사용할 때마다 `math.`을 앞에 붙여야 합니다.

**방법 2: 필요한 것만 가져오기 (`from ... import`)**
```python
from math import pi, sqrt
print(sqrt(16))          # 모듈 이름 없이 바로 사용
print(pi)
```
모듈 이름 없이 바로 사용 가능하지만, import하지 않은 것은 사용할 수 없습니다.

**방법 3: 별칭 사용하기 (`as`)**
```python
import math as m
print(m.sqrt(16))        # 짧은 별칭으로 사용

from math import factorial as fact
print(fact(5))
```
모듈이나 함수 이름이 길 때 짧은 별명을 붙여서 사용합니다.

---

**문제 17. 모범답안:**

```
4
3
8.0
```

- `math.ceil(3.2)` → `4` (올림: 3.2보다 크거나 같은 최소 정수)
- `math.floor(3.8)` → `3` (내림: 3.8보다 작거나 같은 최대 정수)
- `math.pow(2, 3)` → `8.0` (거듭제곱: 2³ = 8, **실수**로 반환)

---

**문제 18. 모범답안:**

- **`random.randint(1, 6)`**: 1 이상 6 이하의 **정수 하나**를 랜덤으로 반환합니다. (예: 주사위)
- **`random.choice(리스트)`**: 리스트에서 요소 **하나**를 랜덤으로 선택하여 반환합니다. (예: 메뉴 추천)
- **`random.shuffle(리스트)`**: 리스트의 요소 **순서를 섞습니다**. 리스트 자체를 변경하며 반환값은 `None`입니다. (예: 카드 섞기)
- **`random.sample(리스트, 개수)`**: 리스트에서 **중복 없이** 지정한 개수만큼 선택하여 **새 리스트**를 반환합니다. 원본은 변경되지 않습니다. (예: 로또 번호)

---

### 🟡 중급

**문제 19. 모범답안:**

`if __name__ == "__main__":` 는 해당 파일이 **직접 실행될 때만** 특정 코드를 실행하도록 합니다.

- **직접 실행** (`python calculator.py`): `__name__` 변수의 값이 `"__main__"`이 됩니다. 따라서 `if __name__ == "__main__":`이 `True`가 되어 그 안의 코드가 실행됩니다.

- **import** (`import calculator`): `__name__` 변수의 값이 모듈 이름인 `"calculator"`가 됩니다. `"calculator" == "__main__"`은 `False`이므로 그 안의 코드가 실행되지 않습니다.

이를 활용하여 모듈 안에 테스트 코드를 넣되, import 시에는 실행되지 않도록 할 수 있습니다.

---

**문제 20. 모범답안:**

```
2025-01-01
2025년 01월 31일
01/08
30
```

- `base = datetime(2025, 1, 1)` → 2025년 1월 1일
- `base + timedelta(days=30)` → 2025년 1월 31일 (1월은 31일까지)
- `base + timedelta(weeks=1)` → 2025년 1월 8일 (7일 후)
- `base.strftime("%Y-%m-%d")` → `"2025-01-01"`
- `after_30.strftime("%Y년 %m월 %d일")` → `"2025년 01월 31일"`
- `after_1week.strftime("%m/%d")` → `"01/08"`
- `(after_30 - base).days` → `30` (두 날짜의 차이 일수)

---

### 🔴 고급

**문제 21. 모범답안:**

**모듈 vs 패키지:**
- **모듈**: 하나의 파이썬 파일(`.py`)입니다. 함수, 변수, 클래스를 포함합니다.
- **패키지**: 모듈을 담는 **폴더**입니다. `__init__.py` 파일이 있어야 패키지로 인식됩니다. 관련된 모듈들을 그룹으로 묶어 관리합니다.

**import 방법 2가지:**

```python
# 방법 1: 모듈 가져오기
from utils import calculator
result = calculator.add(10, 20)  # 30

# 방법 2: 함수 직접 가져오기
from utils.calculator import add
result = add(10, 20)  # 30
```

---

## 💻 실습형 정답

---

### 🟢 초급

**문제 22. 모범답안:**

```python
import math

r = float(input("반지름을 입력하세요: "))

area = math.pi * r ** 2
circumference = 2 * math.pi * r

print(f"원의 넓이: {area:.2f}")
print(f"원의 둘레: {circumference:.2f}")
```

핵심: `math.pi`로 정확한 원주율을 사용하고, `:.2f`로 소수점 2자리까지 출력합니다.

---

**문제 23. 모범답안:**

```python
import random

count = int(input("생성할 세트 수: "))
print()

for i in range(count):
    numbers = random.sample(range(1, 46), 6)
    numbers.sort()
    print(f"[{i+1}세트] {numbers}")
```

핵심: `random.sample(range(1, 46), 6)`으로 1~45 중 중복 없이 6개를 선택합니다. `sort()`로 오름차순 정렬합니다.

---

### 🟡 중급

**문제 24. 모범답안:**

```python
from datetime import datetime

while True:
    try:
        date_str = input("목표 날짜 (YYYY-MM-DD): ")
        target = datetime.strptime(date_str, "%Y-%m-%d")
    except ValueError:
        print("❌ 올바른 형식으로 입력해주세요 (예: 2025-12-25)\n")
    else:
        break

today = datetime.now().replace(hour=0, minute=0, second=0, microsecond=0)
diff = (target - today).days

print(f"오늘: {today.strftime('%Y-%m-%d')}")
print(f"목표: {target.strftime('%Y-%m-%d')}")

if diff > 0:
    print(f"D-{diff} ({diff}일 남았습니다)")
elif diff < 0:
    print(f"D+{abs(diff)} ({abs(diff)}일 전에 지났습니다)")
else:
    print("D-Day! 오늘입니다!")
```

핵심: `datetime.strptime()`으로 문자열을 날짜로 변환합니다. 두 날짜의 차이를 `.days`로 구하여 양수/음수로 미래/과거를 판별합니다. `ValueError`로 잘못된 형식을 처리합니다.

---

**문제 25. 모범답안:**

**string_utils.py:**

```python
# string_utils.py
"""문자열 분석 유틸리티 모듈"""

def count_chars(text):
    """공백 제외 글자 수 반환"""
    return len(text.replace(" ", ""))

def count_words(text):
    """단어 수 반환"""
    return len(text.split())

def find_longest(text):
    """가장 긴 단어 반환"""
    words = text.split()
    longest = ""
    for word in words:
        if len(word) > len(longest):
            longest = word
    return longest

def reverse_words(text):
    """단어 순서 뒤집기"""
    words = text.split()
    words.reverse()
    return " ".join(words)

# 모듈 테스트
if __name__ == "__main__":
    test = "Hello Python World"
    print(f"글자 수: {count_chars(test)}")
    print(f"단어 수: {count_words(test)}")
    print(f"가장 긴 단어: {find_longest(test)}")
    print(f"뒤집기: {reverse_words(test)}")
```

**main.py:**

```python
# main.py
import string_utils

text = input("문장을 입력하세요: ")

print("\n=== 문장 분석 결과 ===")
print(f"글자 수 (공백 제외): {string_utils.count_chars(text)}")
print(f"단어 수: {string_utils.count_words(text)}")
print(f"가장 긴 단어: {string_utils.find_longest(text)}")
print(f"단어 뒤집기: {string_utils.reverse_words(text)}")
```

핵심 포인트:
- 모듈 파일에 관련 함수들을 모아 정리합니다.
- `if __name__ == "__main__":`으로 테스트 코드를 분리합니다.
- 메인 파일에서 `import`하여 `모듈이름.함수이름()`으로 호출합니다.

---

### 🔴 고급

**문제 26. 모범답안:**

```python
import random
from datetime import datetime

print("=== 수학 퀴즈 ===")

total_questions = 5
correct = 0
operators = ["+", "-", "*"]

start_time = datetime.now()
print(f"총 {total_questions}문제 | 시작 시간: {start_time.strftime('%H:%M:%S')}")

results = []

for i in range(total_questions):
    num1 = random.randint(1, 50)
    num2 = random.randint(1, 50)
    op = random.choice(operators)
    
    # 정답 계산
    if op == "+":
        answer = num1 + num2
    elif op == "-":
        answer = num1 - num2
    else:
        answer = num1 * num2
    
    # 문제 출제
    try:
        user_input = input(f"\n[{i+1}/{total_questions}] {num1} {op} {num2} = ? 답: ")
        user_answer = int(user_input)
    except ValueError:
        print("❌ 숫자를 입력해주세요! (오답 처리)")
        results.append({"문제": f"{num1} {op} {num2}", "결과": "오답"})
        continue
    
    if user_answer == answer:
        print("✓ 정답!")
        correct = correct + 1
        results.append({"문제": f"{num1} {op} {num2}", "결과": "정답"})
    else:
        print(f"✗ 오답! (정답: {answer})")
        results.append({"문제": f"{num1} {op} {num2}", "결과": "오답"})

end_time = datetime.now()
elapsed = (end_time - start_time).seconds

# 결과 출력
percent = (correct / total_questions) * 100
print(f"\n=== 결과 ===")
print(f"점수: {correct} / {total_questions} ({percent:.1f}%)")
print(f"소요 시간: {elapsed}초")
```

핵심 포인트:
- `random.randint()`로 랜덤 숫자, `random.choice()`로 랜덤 연산자를 생성합니다.
- `datetime.now()`로 시작/종료 시간을 기록하여 소요 시간을 계산합니다.
- `try-except`로 숫자가 아닌 입력을 처리합니다.
- `if-elif`로 연산자에 따라 정답을 계산합니다.
- 결과를 리스트에 딕셔너리로 저장하여 이력 관리가 가능합니다.

---

조정현 교수 (peterchokr@gmail.com)  
영남이공대학교
