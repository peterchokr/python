# 5장 입력과 출력 — 연습문제

---

## 📝 객관식 문제

---

### 🟢 초급

**문제 1.** `input()` 함수가 반환하는 데이터 타입은?

① int  
② float  
③ str  
④ bool  

---

**문제 2.** 다음 코드에서 사용자가 `25`를 입력했을 때, `age`의 데이터 타입은?

```python
age = input("나이: ")
```

① int  
② float  
③ str  
④ bool  

---

**문제 3.** 다음 코드의 실행 결과는?

```python
print("A", "B", "C")
```

① ABC  
② A B C  
③ A, B, C  
④ (A, B, C)  

---

**문제 4.** `print()` 함수에서 출력 후 줄바꿈을 하지 않으려면 어떤 매개변수를 사용하는가?

① `sep=""`  
② `end=""`  
③ `line=False`  
④ `newline=""`  

---

**문제 5.** 다음 코드의 실행 결과는?

```python
print("2024", "12", "25", sep="-")
```

① 2024 12 25  
② 2024-12-25  
③ 2024/12/25  
④ 20241225  

---

**문제 6.** 사용자 입력을 정수로 받으려면 올바른 코드는?

① `age = input("나이: ")`  
② `age = int("나이: ")`  
③ `age = int(input("나이: "))`  
④ `age = input(int("나이: "))`  

---

**문제 7.** 다음 코드의 실행 결과는?

```python
print("Hello", end="!")
print("World")
```

① Hello! World  
② Hello!World  
③ Hello!\nWorld  
④ Hello! \n World  

---

### 🟡 중급

**문제 8.** 사용자가 `"10 20"`을 입력했을 때, 다음 코드의 실행 결과는?

```python
data = input("입력: ")
a, b = data.split()
print(a + b)
```

① 30  
② 1020  
③ 10 20  
④ 오류 발생  

---

**문제 9.** 다음 코드의 실행 결과는?

```python
print("A", "B", sep="***", end="!\n")
print("C")
```

① A B!\nC  
② A***B!\nC  
③ A***B! (줄바꿈 후) C  
④ A B C  

---

**문제 10.** 사용자가 `"3 5"`를 입력했을 때, 다음 코드의 실행 결과는?

```python
a, b = input("입력: ").split()
result = int(a) * int(b)
print(result)
```

① 35  
② 15  
③ 8  
④ 오류 발생  

---

**문제 11.** 다음 코드의 실행 결과는?

```python
name = "홍길동"
age = 25
print(f"|{name:>10}|{age:<10}|")
```

① `|홍길동       |25        |`  
② `|       홍길동|25        |`  
③ `|홍길동|25|`  
④ 오류 발생  

---

**문제 12.** 다음 중 `print()` 함수의 `sep` 매개변수에 대한 설명으로 올바른 것은?

① 출력의 시작 문자를 지정한다  
② 출력 값들 사이의 구분자를 지정한다  
③ 출력 후 줄바꿈 문자를 지정한다  
④ 출력할 값의 개수를 지정한다  

---

### 🔴 고급

**문제 13.** 다음 코드의 실행 결과로 올바른 것은?

```python
print("Loading", end="")
print(".", ".", ".", sep="", end="")
print("Done!")
```

① Loading . . . Done!  
② Loading...Done!  
③ Loading . . .Done!  
④ Loading... Done!  

---

**문제 14.** 사용자가 `"Kim 25 175.5"`를 입력했을 때, 다음 코드의 실행 결과는?

```python
data = input("입력: ").split()
name = data[0]
age = int(data[1])
height = float(data[2])
print(f"{name}({age}세) - {height}cm")
```

① Kim(25세) - 175.5cm  
② Kim 25 175.5  
③ 오류 발생  
④ Kim(25) - 175.5  

---

**문제 15.** 다음 코드에서 `line` 변수의 값은?

```python
line = "─" * 20
print(len(line))
```

① 20  
② 40  
③ 10  
④ 1  

---

## 📝 주관식 문제

---

### 🟢 초급

**문제 16.** `input()` 함수의 기본 동작을 설명하고, 숫자 계산을 위해 필요한 추가 처리를 설명하시오.

---

**문제 17.** 다음 코드의 실행 결과를 쓰시오.

```python
print("사과", "바나나", "포도", sep=" | ")
print("가격", end=": ")
print("1000원")
```

---

**문제 18.** `print()` 함수의 `sep`과 `end` 매개변수의 기본값과 역할을 각각 설명하시오.

---

### 🟡 중급

**문제 19.** 사용자가 `"홍길동 20"`을 입력했을 때, 다음 코드의 실행 결과를 쓰고 동작 과정을 설명하시오.

```python
data = input("이름 나이: ").split()
name = data[0]
age = int(data[1])
print(f"{name}님은 {age}세이고, 내년에 {age + 1}세가 됩니다.")
```

---

**문제 20.** f-string의 정렬 옵션 `<`, `>`, `^`의 의미를 각각 설명하고, 다음 코드의 실행 결과를 쓰시오.

```python
word = "Hi"
print(f"[{word:<8}]")
print(f"[{word:>8}]")
print(f"[{word:^8}]")
```

---

### 🔴 고급

**문제 21.** 다음 코드의 실행 결과를 정확하게 쓰시오. (공백과 줄바꿈에 주의)

```python
print("=" * 20)
print("이름", "나이", sep="\t")
print("-" * 20)
print("홍길동", end="\t")
print(25)
print("=" * 20)
```

---

## 💻 실습형 문제

---

### 🟢 초급

**문제 22.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> `sep` 매개변수를 활용하여 전화번호 형식으로 출력하시오.

출력:

```
010-1234-5678
```

---

**문제 23.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> `end` 매개변수를 활용하여 아래와 같이 한 줄에 출력하시오.

출력:

```
[■■■■■■■■■■] 100%
```

---

**문제 24.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 변수 `name = "홍길동"`, `age = 20`, `major = "컴퓨터공학"`을 사용하여 아래와 같이 출력하시오. (문자열 반복 연산자와 f-string 활용)

출력:

```
====================
    학생 정보 카드
====================
이름: 홍길동
나이: 20세
전공: 컴퓨터공학
====================
```

---

### 🟡 중급

**문제 25.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 사용자로부터 **상품명, 단가, 수량**을 `input()`으로 입력받아 아래와 같은 형식의 간이 영수증을 출력하시오.  
> 천 단위 구분자(`,`)와 정렬을 활용하시오.

출력 예시 (상품명: 노트북, 단가: 1500000, 수량: 2):

```
========================================
           간이 영수증
========================================
상품명:                        노트북
단가:                     1,500,000원
수량:                            2개
----------------------------------------
합계:                     3,000,000원
========================================
```

---

### 🔴 고급

**문제 26.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 사용자로부터 **이름, 국어 영어 수학 점수**(공백 구분)를 한 줄로 입력받아(`split()` 활용) 아래와 같은 성적표를 출력하시오.  
> 평균은 소수점 첫째 자리까지 표시하시오.

입력 예시: `홍길동 85 92 78`

출력 예시:

```
╔══════════════════════════════════════╗
║            성적 통지표               ║
╠══════════════════════════════════════╣
║  이름: 홍길동                        ║
╠══════════════════════════════════════╣
║  과목          점수                  ║
║  ─────────────────────              ║
║  국어            85점                ║
║  영어            92점                ║
║  수학            78점                ║
╠══════════════════════════════════════╣
║  총점           255점                ║
║  평균          85.0점                ║
╚══════════════════════════════════════╝
```

---
---

# 🔑 정답 및 해설

---

## 📝 객관식 정답

---

### 🟢 초급

**문제 1. 정답: ③ str**

`input()` 함수는 사용자가 어떤 값을 입력하더라도 항상 문자열(str) 타입으로 반환합니다. 숫자를 입력해도 문자열입니다.

---

**문제 2. 정답: ③ str**

`input()` 함수의 반환값은 항상 문자열입니다. 사용자가 `25`를 입력해도 `age`에는 문자열 `"25"`가 저장됩니다. 정수로 사용하려면 `int(input("나이: "))`로 변환해야 합니다.

---

**문제 3. 정답: ② A B C**

`print()` 함수에서 여러 값을 쉼표로 구분하면, 기본적으로 **공백 한 칸**으로 구분하여 출력합니다. `sep` 매개변수의 기본값이 `" "`(공백)이기 때문입니다.

---

**문제 4. 정답: ② end=""**

`end` 매개변수의 기본값은 `"\n"`(줄바꿈)입니다. `end=""`로 설정하면 줄바꿈 없이 출력됩니다. `sep`은 값들 사이의 구분자를 지정하는 매개변수입니다.

---

**문제 5. 정답: ② 2024-12-25**

`sep="-"`는 출력할 값들 사이를 `-`로 연결합니다. 따라서 `"2024"`, `"12"`, `"25"`가 하이픈으로 연결되어 `2024-12-25`가 출력됩니다.

---

**문제 6. 정답: ③ `int(input("나이: "))`**

`input()`으로 문자열을 받은 후 `int()`로 정수 변환하는 것이 올바른 순서입니다. `input()`이 안쪽에서 먼저 실행되고, 그 결과를 `int()`가 변환합니다.

---

**문제 7. 정답: ② Hello!World**

첫 번째 `print("Hello", end="!")`는 `Hello!`를 출력하고 줄바꿈하지 않습니다. 두 번째 `print("World")`는 바로 이어서 `World`를 출력합니다. 따라서 `Hello!World`가 됩니다.

---

### 🟡 중급

**문제 8. 정답: ② 1020**

`split()`으로 분리한 `a`와 `b`는 여전히 **문자열**입니다. 문자열 `"10"` + `"20"` = `"1020"` (문자열 연결). 숫자 덧셈을 하려면 `int(a) + int(b)`로 변환해야 합니다.

---

**문제 9. 정답: ③ A***B! (줄바꿈 후) C**

`sep="***"`로 A와 B 사이에 `***`가 들어가고, `end="!\n"`으로 끝에 `!`와 줄바꿈이 붙습니다. 따라서 첫 줄에 `A***B!`, 다음 줄에 `C`가 출력됩니다.

---

**문제 10. 정답: ② 15**

`split()`으로 `"3"`과 `"5"`로 분리하고, `int()`로 각각 정수 3과 5로 변환합니다. `3 * 5 = 15`가 출력됩니다.

---

**문제 11. 정답: ② `|       홍길동|25        |`**

`{name:>10}`은 10칸에서 오른쪽 정렬(앞에 공백 채움), `{age:<10}`은 10칸에서 왼쪽 정렬(뒤에 공백 채움)입니다. 단, 한글은 터미널에서 2바이트로 표시되어 실제 정렬이 다를 수 있지만, 파이썬 내부적으로는 문자 수 기준으로 정렬합니다.

---

**문제 12. 정답: ② 출력 값들 사이의 구분자를 지정한다**

`sep`은 separator(구분자)의 약자로, `print()` 함수에서 여러 값 사이에 삽입할 문자를 지정합니다. 기본값은 공백(`" "`)입니다.

---

### 🔴 고급

**문제 13. 정답: ② Loading...Done!**

첫 번째 `print("Loading", end="")`는 `Loading`을 출력하고 줄바꿈 없음. 두 번째 `print(".", ".", ".", sep="", end="")`는 점 3개를 구분자 없이 붙여서 `...`을 출력하고 줄바꿈 없음. 세 번째 `print("Done!")`이 이어서 출력. 최종: `Loading...Done!`

---

**문제 14. 정답: ① Kim(25세) - 175.5cm**

`split()`으로 `["Kim", "25", "175.5"]`로 분리한 뒤, `data[1]`을 `int()`로, `data[2]`를 `float()`로 변환합니다. f-string으로 포매팅하면 `Kim(25세) - 175.5cm`이 출력됩니다.

---

**문제 15. 정답: ① 20**

`"─" * 20`은 `─` 문자를 20번 반복합니다. `len()`은 문자열의 **문자 수**를 반환하므로 결과는 20입니다. `─`는 특수문자이지만 1개의 문자로 취급됩니다.

---

## 📝 주관식 정답

---

### 🟢 초급

**문제 16. 모범답안:**

`input()` 함수는 사용자로부터 키보드 입력을 받아 **항상 문자열(str) 타입**으로 반환합니다. 괄호 안에 프롬프트 메시지를 넣으면 사용자에게 안내 문구를 표시할 수 있습니다.

숫자 계산을 위해서는 타입 변환이 필요합니다:
- 정수 계산: `int(input("숫자: "))` → str을 int로 변환
- 실수 계산: `float(input("숫자: "))` → str을 float로 변환

---

**문제 17. 모범답안:**

```
사과 | 바나나 | 포도
가격: 1000원
```

첫 번째 줄: `sep=" | "`로 세 과일 사이에 ` | `가 삽입됩니다. 두 번째 줄: `end=": "`로 "가격" 뒤에 줄바꿈 대신 `: `가 붙고, 세 번째 `print`가 이어서 출력됩니다.

---

**문제 18. 모범답안:**

**`sep` (separator, 구분자):**
- 기본값: `" "` (공백 한 칸)
- 역할: `print()`에서 여러 값을 출력할 때, **값들 사이**에 삽입되는 문자를 지정합니다.
- 예: `print("A", "B", sep="-")` → `A-B`

**`end` (끝 문자):**
- 기본값: `"\n"` (줄바꿈)
- 역할: `print()`가 출력을 마친 후 **맨 끝**에 붙는 문자를 지정합니다.
- 예: `print("Hello", end="!")` → `Hello!` (줄바꿈 없음)

---

### 🟡 중급

**문제 19. 모범답안:**

실행 결과:

```
홍길동님은 20세이고, 내년에 21세가 됩니다.
```

**동작 과정:**
1. `input()`으로 `"홍길동 20"` 문자열을 입력받습니다.
2. `.split()`으로 공백 기준 분리 → `["홍길동", "20"]` 리스트 생성
3. `data[0]` → `"홍길동"` (name에 할당)
4. `int(data[1])` → `"20"`을 정수 20으로 변환 (age에 할당)
5. f-string에서 `age + 1` → `20 + 1 = 21`이 계산되어 출력

---

**문제 20. 모범답안:**

**정렬 옵션 설명:**
- `<` : 왼쪽 정렬 (기본값, 뒤에 공백 채움)
- `>` : 오른쪽 정렬 (앞에 공백 채움)
- `^` : 가운데 정렬 (양쪽에 공백 채움)

**실행 결과:**

```
[Hi      ]
[      Hi]
[   Hi   ]
```

8칸 너비에서 `"Hi"`(2자)를 각각 왼쪽, 오른쪽, 가운데에 정렬합니다.

---

### 🔴 고급

**문제 21. 모범답안:**

```
====================
이름	나이
--------------------
홍길동	25
====================
```

**단계별 설명:**
1. `"=" * 20` → `====================` 출력 후 줄바꿈
2. `"이름"`, `"나이"`를 `sep="\t"`(탭)으로 구분하여 출력 후 줄바꿈
3. `"-" * 20` → `--------------------` 출력 후 줄바꿈
4. `"홍길동"` 출력 후 `end="\t"`로 줄바꿈 대신 탭
5. `25` 출력 후 기본 줄바꿈
6. `"=" * 20` → `====================` 출력

---

## 💻 실습형 정답

---

### 🟢 초급

**문제 22. 모범답안:**

```python
print("010", "1234", "5678", sep="-")
```

핵심: `sep="-"`로 세 값 사이에 하이픈을 자동 삽입합니다.

---

**문제 23. 모범답안:**

```python
print("[", end="")
print("■" * 10, end="")
print("] 100%")
```

핵심: `end=""`로 줄바꿈을 없애서 세 번의 `print()`를 한 줄에 연결합니다.

---

**문제 24. 모범답안:**

```python
name = "홍길동"
age = 20
major = "컴퓨터공학"

line = "=" * 20
print(line)
print("    학생 정보 카드")
print(line)
print(f"이름: {name}")
print(f"나이: {age}세")
print(f"전공: {major}")
print(line)
```

핵심: `"=" * 20`으로 구분선을 만들고 f-string으로 변수를 출력합니다.

---

### 🟡 중급

**문제 25. 모범답안:**

```python
# 입력
product = input("상품명: ")
unit_price = int(input("단가: "))
quantity = int(input("수량: "))

# 계산
total = unit_price * quantity

# 출력
w = 40
print("=" * w)
print(f"{'간이 영수증':^{w}}")
print("=" * w)
print(f"상품명: {product:>{w - 8}}")
print(f"단가: {unit_price:>{w - 7},}원")
print(f"수량: {quantity:>{w - 7}}개")
print("-" * w)
print(f"합계: {total:>{w - 7},}원")
print("=" * w)
```

핵심 포인트:
- `{:>칸수,}`로 오른쪽 정렬 + 천 단위 구분자를 동시에 적용합니다.
- `{:^칸수}`로 제목을 가운데 정렬합니다.
- 문자열 반복(`*`)으로 구분선을 만듭니다.

---

### 🔴 고급

**문제 26. 모범답안:**

```python
# 한 줄 입력 받아 분리
data = input("이름 국어 영어 수학: ").split()
name = data[0]
korean = int(data[1])
english = int(data[2])
math = int(data[3])

# 계산
total = korean + english + math
average = total / 3

# 성적표 출력
w = 38
print("╔" + "═" * w + "╗")
print("║" + "성적 통지표".center(w) + "║")
print("╠" + "═" * w + "╣")
print(f"║  이름: {name}".ljust(w + 1) + "║")
print("╠" + "═" * w + "╣")
print(f"║  {'과목':<10}{'점수':>10}".ljust(w + 1) + "║")
print(f"║  {'─' * 20}".ljust(w + 1) + "║")
print(f"║  {'국어':<10}{korean:>10}점".ljust(w + 1) + "║")
print(f"║  {'영어':<10}{english:>10}점".ljust(w + 1) + "║")
print(f"║  {'수학':<10}{math:>10}점".ljust(w + 1) + "║")
print("╠" + "═" * w + "╣")
print(f"║  {'총점':<10}{total:>10}점".ljust(w + 1) + "║")
print(f"║  {'평균':<10}{average:>10.1f}점".ljust(w + 1) + "║")
print("╚" + "═" * w + "╝")
```

핵심 포인트:
- `split()`으로 한 줄 입력을 여러 변수로 분리합니다.
- `ljust()`로 박스 너비를 맞추고, f-string 정렬 옵션으로 내부 항목을 정렬합니다.
- `:.1f`로 평균을 소수점 첫째 자리까지 표시합니다.
- 박스 문자(`╔═╗║╠╣╚╝`)로 테두리를 만듭니다.

---

조정현 교수 (peterchokr@gmail.com)  
영남이공대학교
