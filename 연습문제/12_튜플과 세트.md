# 12장 튜플과 세트 — 연습문제

---

## 📝 객관식 문제

---

### 🟢 초급

**문제 1.** 튜플을 생성하는 올바른 방법은?

① `t = [1, 2, 3]`
② `t = (1, 2, 3)`
③ `t = {1, 2, 3}`
④ `t = <1, 2, 3>`

---

**문제 2.** 다음 코드의 실행 결과는?

```python
t = (10, 20, 30)
print(t[1])
```

① 10
② 20
③ 30
④ 오류 발생

---

**문제 3.** 다음 코드의 실행 결과는?

```python
t = (10, 20, 30)
t[0] = 100
print(t)
```

① `(100, 20, 30)`
② `(10, 20, 30)`
③ `[100, 20, 30]`
④ 오류 발생

---

**문제 4.** 다음 중 항목이 하나인 튜플을 올바르게 생성한 것은?

① `t = (42)`
② `t = (42,)`
③ `t = [42]`
④ `t = {42}`

---

**문제 5.** 다음 코드의 실행 결과는?

```python
numbers = {1, 2, 2, 3, 3, 3}
print(numbers)
```

① `{1, 2, 2, 3, 3, 3}`
② `{1, 2, 3}`
③ `[1, 2, 3]`
④ 오류 발생

---

**문제 6.** 빈 세트를 올바르게 생성하는 방법은?

① `s = {}`
② `s = set()`
③ `s = ()`
④ `s = []`

---

**문제 7.** 다음 코드의 실행 결과는?

```python
fruits = {"사과", "바나나"}
fruits.add("오렌지")
fruits.add("사과")
print(len(fruits))
```

① 2
② 3
③ 4
④ 오류 발생

---

### 🟡 중급

**문제 8.** 다음 코드의 실행 결과는?

```python
point = (10, 20)
x, y = point
print(x + y)
```

① `(10, 20)`
② 30
③ `1020`
④ 오류 발생

---

**문제 9.** 다음 코드의 실행 결과는?

```python
a = 5
b = 10
a, b = b, a
print(a, b)
```

① `5 10`
② `10 5`
③ `10 10`
④ 오류 발생

---

**문제 10.** 다음 코드의 실행 결과는?

```python
a = {1, 2, 3}
b = {3, 4, 5}
print(a | b)
```

① `{3}`
② `{1, 2}`
③ `{1, 2, 3, 4, 5}`
④ `{4, 5}`

---

**문제 11.** 다음 코드의 실행 결과는?

```python
a = {1, 2, 3, 4}
b = {3, 4, 5, 6}
print(a & b)
```

① `{1, 2, 3, 4, 5, 6}`
② `{3, 4}`
③ `{1, 2}`
④ `{5, 6}`

---

**문제 12.** 다음 코드의 실행 결과는?

```python
a = {1, 2, 3, 4}
b = {3, 4, 5, 6}
print(a - b)
```

① `{1, 2}`
② `{5, 6}`
③ `{3, 4}`
④ `{1, 2, 5, 6}`

---

### 🔴 고급

**문제 13.** 다음 코드의 실행 결과는?

```python
colors = {"빨강", "파랑", "초록"}
colors.discard("노랑")
colors.remove("파랑")
print(len(colors))
```

① 1
② 2
③ 3
④ 오류 발생

---

**문제 14.** 다음 코드의 실행 결과는?

```python
a = {1, 2}
b = {1, 2, 3, 4}
print(a.issubset(b))
print(b.issuperset(a))
```

① `True` `False`
② `False` `True`
③ `True` `True`
④ `False` `False`

---

**문제 15.** 다음 코드의 실행 결과는?

```python
data = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5]
unique = set(data)
result = sorted(unique)
print(result)
```

① `{1, 2, 3, 4, 5, 6, 9}`
② `[1, 2, 3, 4, 5, 6, 9]`
③ `[3, 1, 4, 5, 9, 2, 6]`
④ `(1, 2, 3, 4, 5, 6, 9)`

---

## 📝 주관식 문제

---

### 🟢 초급

**문제 16.** 튜플과 리스트의 공통점과 차이점을 각각 2가지 이상 설명하시오.

---

**문제 17.** 세트(Set)의 특징을 3가지 설명하고, 리스트 `[1, 2, 2, 3, 3, 4]`를 세트로 변환한 결과를 쓰시오.

---

**문제 18.** 다음 코드의 실행 결과를 쓰시오.

```python
info = ("김철수", 20, "서울")
name, age, city = info
print(f"{name}은 {age}세이고 {city}에 삽니다.")
```

---

### 🟡 중급

**문제 19.** 세트의 `remove()`와 `discard()`의 차이점을 설명하고, 다음 코드의 실행 결과를 쓰시오.

```python
colors = {"빨강", "파랑", "초록", "노랑"}
colors.discard("보라")
colors.remove("노랑")
print(colors)
print(len(colors))
```

---

**문제 20.** 다음 코드의 실행 결과를 쓰고, 합집합·교집합·차집합 각 연산의 의미를 설명하시오.

```python
korean = {"김철수", "박영희", "이민수", "최지은"}
math_class = {"박영희", "최지은", "홍길동", "강수진"}

both = korean & math_class
only_korean = korean - math_class
all_students = korean | math_class

print(f"둘 다 수강: {both}")
print(f"국어만 수강: {only_korean}")
print(f"전체 학생 수: {len(all_students)}명")
```

---

### 🔴 고급

**문제 21.** 다음 코드의 실행 결과를 쓰고, 각 단계의 동작을 설명하시오.

```python
scores = [
    ("김철수", 85),
    ("박영희", 92),
    ("이민수", 78),
    ("최지은", 92),
    ("홍길동", 85)
]

# 고유 점수 추출
unique_scores = set()
for name, score in scores:
    unique_scores.add(score)

print(f"고유 점수: {sorted(unique_scores)}")
print(f"고유 점수 개수: {len(unique_scores)}개")

# 90점 이상 학생
top_students = []
for name, score in scores:
    if score >= 90:
        top_students.append(name)

print(f"90점 이상: {top_students}")
```

---

## 💻 실습형 문제

---

### 🟢 초급

**문제 22.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 3개 도시의 정보를 튜플로 저장하고 출력하시오.각 튜플은 `(도시명, 국가, 인구)`로 구성합니다.
>
> - 서울, 대한민국, 9776000
> - 도쿄, 일본, 13960000
> - 뉴욕, 미국, 8336000

출력 예시:

```
=== 세계 도시 정보 ===
1. 서울 (대한민국) - 인구: 9,776,000명
2. 도쿄 (일본) - 인구: 13,960,000명
3. 뉴욕 (미국) - 인구: 8,336,000명
```

---

**문제 23.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 리스트 `items = ["사과", "바나나", "사과", "오렌지", "바나나", "사과", "포도"]`에서
> 세트를 활용하여 중복을 제거하고, 고유 항목과 각 항목의 개수를 출력하시오.

출력 예시:

```
원본: ['사과', '바나나', '사과', '오렌지', '바나나', '사과', '포도']
전체 개수: 7개
고유 항목 수: 4개

항목별 개수:
- 사과: 3개
- 바나나: 2개
- 오렌지: 1개
- 포도: 1개
```

---

### 🟡 중급

**문제 24.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 두 동아리의 회원 명단을 세트로 관리하고 분석하시오.
>
> - 코딩 동아리: `{"김철수", "박영희", "이민수", "최지은", "홍길동"}`
> - 음악 동아리: `{"박영희", "최지은", "강수진", "정민호"}`
>
> 출력: 두 동아리 모두 가입한 회원, 코딩만 가입한 회원, 음악만 가입한 회원, 전체 회원 수

출력 예시:

```
=== 동아리 회원 분석 ===

코딩 동아리: 5명
음악 동아리: 4명

양쪽 모두 가입: 박영희, 최지은
코딩만 가입: 김철수, 이민수, 홍길동
음악만 가입: 강수진, 정민호

전체 회원 수: 7명
```

---

**문제 25.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 5명의 학생 정보를 튜플로 입력받아 리스트에 저장하고, 분석 결과를 출력하시오.
> 각 학생 정보는 `(이름, 나이, 전공)` 형태의 튜플입니다.
>
> 출력: 전체 학생 정보, 평균 나이, 전공별 학생 수

출력 예시 (입력: 김철수/20/컴퓨터, 박영희/21/경영, 이민수/22/컴퓨터, 최지은/20/디자인, 홍길동/21/컴퓨터):

```
=== 학생 정보 ===
1. 김철수 (20세, 컴퓨터)
2. 박영희 (21세, 경영)
3. 이민수 (22세, 컴퓨터)
4. 최지은 (20세, 디자인)
5. 홍길동 (21세, 컴퓨터)

평균 나이: 20.8세

전공별 학생 수:
- 컴퓨터: 3명
- 경영: 1명
- 디자인: 1명
```

---

### 🔴 고급

**문제 26.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 다음 과목별 수강생 목록을 세트로 관리하고 종합 분석을 수행하시오.
>
> - 파이썬: `{"김철수", "박영희", "이민수", "최지은", "홍길동"}`
> - 데이터베이스: `{"박영희", "이민수", "강수진", "정민호", "최지은"}`
> - 웹개발: `{"김철수", "강수진", "최지은", "한지우"}`
>
> 분석 항목:
>
> 1) 세 과목 모두 수강하는 학생
> 2) 정확히 한 과목만 수강하는 학생
> 3) 파이썬은 듣지만 데이터베이스는 듣지 않는 학생
> 4) 전체 수강생 수 (중복 제거)
> 5) 각 학생이 수강하는 과목 수

출력 예시:

```
=== 수강 분석 ===

1) 세 과목 모두 수강: 최지은
2) 한 과목만 수강: 홍길동, 정민호, 한지우
3) 파이썬O, 데이터베이스X: 김철수, 홍길동
4) 전체 수강생: 8명

5) 학생별 수강 과목 수:
- 김철수: 2과목
- 박영희: 2과목
- 이민수: 2과목
- 최지은: 3과목
- 홍길동: 1과목
- 강수진: 2과목
- 정민호: 1과목
- 한지우: 1과목
```

---

---

# 🔑 정답 및 해설

---

## 📝 객관식 정답

---

### 🟢 초급

**문제 1. 정답: ② `t = (1, 2, 3)`**

튜플은 **소괄호 `()`**로 생성합니다. `[]`는 리스트, `{}`는 세트 또는 딕셔너리를 만드는 괄호입니다.

---

**문제 2. 정답: ② 20**

튜플도 리스트와 마찬가지로 인덱스로 접근합니다. `t[1]`은 두 번째 항목인 `20`입니다.

---

**문제 3. 정답: ④ 오류 발생**

튜플은 **불변(immutable)**이므로 항목을 변경할 수 없습니다. `t[0] = 100`은 `TypeError`가 발생합니다.

---

**문제 4. 정답: ② `t = (42,)`**

항목이 하나인 튜플은 **쉼표가 필수**입니다. `(42)`는 괄호 안의 숫자 42와 같으므로 `int` 타입이 됩니다.

---

**문제 5. 정답: ② `{1, 2, 3}`**

세트는 **중복을 허용하지 않습니다**. 중복된 값은 자동으로 제거되어 `{1, 2, 3}`만 남습니다.

---

**문제 6. 정답: ② `s = set()`**

`{}`는 빈 **딕셔너리**를 생성합니다. 빈 세트는 반드시 `set()` 함수를 사용해야 합니다.

---

**문제 7. 정답: ② 3**

`add("오렌지")`로 3개가 되고, `add("사과")`는 이미 존재하므로 무시됩니다. 최종 세트는 `{"사과", "바나나", "오렌지"}`로 길이는 3입니다.

---

### 🟡 중급

**문제 8. 정답: ② 30**

튜플 언패킹으로 `x = 10`, `y = 20`이 됩니다. `x + y = 30`이 출력됩니다.

---

**문제 9. 정답: ② `10 5`**

`a, b = b, a`는 튜플 언패킹을 이용한 **값 교환**입니다. 오른쪽의 `b, a`가 먼저 `(10, 5)` 튜플로 만들어진 후, 왼쪽 `a, b`에 언패킹됩니다.

---

**문제 10. 정답: ③ `{1, 2, 3, 4, 5}`**

`|`는 **합집합** 연산자입니다. 두 세트의 모든 원소를 합치되 중복을 제거합니다.

---

**문제 11. 정답: ② `{3, 4}`**

`&`는 **교집합** 연산자입니다. 두 세트에 **공통으로** 존재하는 원소만 반환합니다.

---

**문제 12. 정답: ① `{1, 2}`**

`-`는 **차집합** 연산자입니다. `a - b`는 `a`에는 있지만 `b`에는 없는 원소를 반환합니다.

---

### 🔴 고급

**문제 13. 정답: ② 2**

`discard("노랑")`은 `"노랑"`이 없어도 **오류가 발생하지 않습니다**. `remove("파랑")`으로 `"파랑"`이 제거됩니다. 결과: `{"빨강", "초록"}`, 길이는 2.

---

**문제 14. 정답: ③ `True` `True`**

`{1, 2}`는 `{1, 2, 3, 4}`의 **부분집합**(subset)이므로 `issubset()` → `True`. `{1, 2, 3, 4}`는 `{1, 2}`의 **상위집합**(superset)이므로 `issuperset()` → `True`.

---

**문제 15. 정답: ② `[1, 2, 3, 4, 5, 6, 9]`**

`set(data)`로 중복이 제거되고, `sorted()`는 세트를 정렬하여 **리스트로 반환**합니다. `sorted()`는 원본을 변경하지 않고 새 리스트를 만드는 내장 함수입니다.

---

## 📝 주관식 정답

---

### 🟢 초급

**문제 16. 모범답안:**

**공통점:**

- 여러 개의 값을 순서대로 저장할 수 있습니다.
- 인덱싱과 슬라이싱을 사용할 수 있습니다.
- `for` 반복문으로 순회할 수 있습니다.

**차이점:**

- 리스트는 `[]`로 생성, 튜플은 `()`로 생성합니다.
- 리스트는 **변경 가능(mutable)**, 튜플은 **변경 불가(immutable)**입니다.
- 리스트는 `append()`, `remove()` 등 수정 메서드가 있지만, 튜플은 수정 메서드가 없습니다.
- 튜플이 리스트보다 처리 속도가 빠릅니다.

---

**문제 17. 모범답안:**

**세트의 특징 3가지:**

1. **중복을 허용하지 않습니다** — 같은 값을 여러 번 추가해도 하나만 저장됩니다.
2. **순서가 없습니다** — 인덱스로 접근할 수 없고, 출력 순서가 일정하지 않습니다.
3. **집합 연산을 지원합니다** — 합집합(`|`), 교집합(`&`), 차집합(`-`) 등을 사용할 수 있습니다.

**변환 결과:**

```python
set([1, 2, 2, 3, 3, 4])  # → {1, 2, 3, 4}
```

---

**문제 18. 모범답안:**

```
김철수은 20세이고 서울에 삽니다.
```

튜플 `info`의 세 값이 `name`, `age`, `city` 변수에 각각 **언패킹**됩니다. 이를 통해 한 번에 여러 변수에 값을 할당할 수 있습니다.

---

### 🟡 중급

**문제 19. 모범답안:**

- `remove(값)`: 해당 값을 제거합니다. 값이 **없으면 `KeyError` 오류**가 발생합니다.
- `discard(값)`: 해당 값을 제거합니다. 값이 **없어도 오류가 발생하지 않습니다**.

**실행 결과:**

```
{'빨강', '파랑', '초록'}
3
```

1. `discard("보라")` → `"보라"`가 없지만 오류 없이 무시됨 → 4개 유지
2. `remove("노랑")` → `"노랑"` 제거 → 3개

(주의: 세트는 순서가 없으므로 출력 순서는 다를 수 있습니다.)

---

**문제 20. 모범답안:**

**집합 연산의 의미:**

- **합집합(`|`)**: 두 세트의 **모든** 원소를 합칩니다 (중복 제거).
- **교집합(`&`)**: 두 세트에 **공통으로** 존재하는 원소만 반환합니다.
- **차집합(`-`)**: 첫 번째 세트에는 있지만 두 번째 세트에는 **없는** 원소를 반환합니다.

**실행 결과:**

```
둘 다 수강: {'박영희', '최지은'}
국어만 수강: {'김철수', '이민수'}
전체 학생 수: 6명
```

(주의: 세트 출력 순서는 다를 수 있습니다.)

- `korean & math_class` → 공통: `{"박영희", "최지은"}`
- `korean - math_class` → 국어에만 있는: `{"김철수", "이민수"}`
- `korean | math_class` → 전체 6명: `{"김철수", "박영희", "이민수", "최지은", "홍길동", "강수진"}`

---

### 🔴 고급

**문제 21. 모범답안:**

**실행 결과:**

```
고유 점수: [78, 85, 92]
고유 점수 개수: 3개
90점 이상: ['박영희', '최지은']
```

**단계별 동작:**

1. 리스트 `scores`에 5명의 `(이름, 점수)` 튜플이 저장됩니다.
2. for문에서 튜플 언패킹(`name, score = ...`)으로 이름과 점수를 분리합니다.
3. `unique_scores` 세트에 점수를 `add()`하면, 중복된 85와 92는 한 번만 저장됩니다 → `{85, 92, 78}`.
4. `sorted(unique_scores)`로 세트를 정렬한 **리스트** `[78, 85, 92]`를 반환합니다.
5. 두 번째 for문에서 점수가 90 이상인 학생(`박영희`: 92, `최지은`: 92)을 `top_students`에 추가합니다.

---

## 💻 실습형 정답

---

### 🟢 초급

**문제 22. 모범답안:**

```python
# 도시 정보를 튜플로 저장
seoul = ("서울", "대한민국", 9776000)
tokyo = ("도쿄", "일본", 13960000)
newyork = ("뉴욕", "미국", 8336000)

cities = [seoul, tokyo, newyork]

print("=== 세계 도시 정보 ===")
for i in range(len(cities)):
    name, country, population = cities[i]  # 튜플 언패킹
    print(f"{i+1}. {name} ({country}) - 인구: {population:,}명")
```

핵심: 고정된 데이터를 튜플로 저장하고, 리스트에 담아 반복문으로 처리합니다. 튜플 언패킹으로 각 값을 변수에 할당합니다.

---

**문제 23. 모범답안:**

```python
items = ["사과", "바나나", "사과", "오렌지", "바나나", "사과", "포도"]

# 세트로 중복 제거
unique = set(items)

print(f"원본: {items}")
print(f"전체 개수: {len(items)}개")
print(f"고유 항목 수: {len(unique)}개")

print("\n항목별 개수:")
for item in unique:
    count = items.count(item)
    print(f"- {item}: {count}개")
```

핵심: `set()`으로 중복을 제거하고, 원본 리스트의 `count()` 메서드로 각 항목의 개수를 셉니다.

---

### 🟡 중급

**문제 24. 모범답안:**

```python
coding = {"김철수", "박영희", "이민수", "최지은", "홍길동"}
music = {"박영희", "최지은", "강수진", "정민호"}

# 세트 연산
both = coding & music          # 교집합
only_coding = coding - music   # 차집합
only_music = music - coding    # 차집합
all_members = coding | music   # 합집합

print("=== 동아리 회원 분석 ===\n")
print(f"코딩 동아리: {len(coding)}명")
print(f"음악 동아리: {len(music)}명")

print(f"\n양쪽 모두 가입: {', '.join(both)}")
print(f"코딩만 가입: {', '.join(only_coding)}")
print(f"음악만 가입: {', '.join(only_music)}")
print(f"\n전체 회원 수: {len(all_members)}명")
```

핵심 포인트:

- `&` 교집합으로 공통 회원을 찾습니다.
- `-` 차집합으로 한쪽에만 속한 회원을 찾습니다.
- `|` 합집합으로 전체 회원 수를 구합니다 (중복 자동 제거).
- `', '.join()`으로 세트 내용을 보기 좋게 출력합니다.

---

**문제 25. 모범답안:**

```python
students = []

for i in range(5):
    name = input(f"{i+1}번 학생 이름: ")
    age = int(input(f"{i+1}번 학생 나이: "))
    major = input(f"{i+1}번 학생 전공: ")
    students.append((name, age, major))  # 튜플로 저장

# 학생 정보 출력
print("\n=== 학생 정보 ===")
for i in range(len(students)):
    name, age, major = students[i]
    print(f"{i+1}. {name} ({age}세, {major})")

# 평균 나이
total_age = 0
for name, age, major in students:
    total_age = total_age + age
avg_age = total_age / len(students)
print(f"\n평균 나이: {avg_age}세")

# 전공별 학생 수
majors = set()
for name, age, major in students:
    majors.add(major)

print("\n전공별 학생 수:")
for m in majors:
    count = 0
    for name, age, major in students:
        if major == m:
            count = count + 1
    print(f"- {m}: {count}명")
```

핵심 포인트:

- 각 학생 정보를 튜플로 묶어 리스트에 저장합니다.
- 튜플 언패킹으로 for문에서 이름, 나이, 전공을 분리합니다.
- 세트로 고유 전공을 추출하고, 각 전공의 학생 수를 셉니다.

---

### 🔴 고급

**문제 26. 모범답안:**

```python
python_class = {"김철수", "박영희", "이민수", "최지은", "홍길동"}
db_class = {"박영희", "이민수", "강수진", "정민호", "최지은"}
web_class = {"김철수", "강수진", "최지은", "한지우"}

# 1) 세 과목 모두 수강
all_three = python_class & db_class & web_class
print("=== 수강 분석 ===\n")
print(f"1) 세 과목 모두 수강: {', '.join(all_three)}")

# 전체 수강생
all_students = python_class | db_class | web_class

# 2) 정확히 한 과목만 수강
one_only = set()
for student in all_students:
    count = 0
    if student in python_class:
        count = count + 1
    if student in db_class:
        count = count + 1
    if student in web_class:
        count = count + 1
    if count == 1:
        one_only.add(student)
print(f"2) 한 과목만 수강: {', '.join(one_only)}")

# 3) 파이썬O, 데이터베이스X
python_not_db = python_class - db_class
print(f"3) 파이썬O, 데이터베이스X: {', '.join(python_not_db)}")

# 4) 전체 수강생 수
print(f"4) 전체 수강생: {len(all_students)}명")

# 5) 학생별 수강 과목 수
print(f"\n5) 학생별 수강 과목 수:")
for student in all_students:
    count = 0
    if student in python_class:
        count = count + 1
    if student in db_class:
        count = count + 1
    if student in web_class:
        count = count + 1
    print(f"- {student}: {count}과목")
```

핵심 포인트:

- `&`를 연속 사용하여 세 세트의 공통 원소(교집합)를 구합니다.
- `|`로 전체 수강생을 구하고, 각 학생이 몇 개 세트에 속하는지 `in` 연산자로 확인합니다.
- 차집합(`-`)으로 특정 과목 수강/미수강 조건을 처리합니다.
- 세트의 `in` 연산자는 리스트보다 매우 빠르게 존재 여부를 확인합니다.

---


수고했습니다.   
조정현 교수([peterchokr@gmail.com](mailto:peterchokr@gmail.com)) 영남이공대학교

이 연습문제는 Claude 및 Gemini와 협업으로 제작되었습니다.
