# 9장 함수 1 (기본) — 연습문제

---

## 📝 객관식 문제

---

### 🟢 초급

**문제 1.** 파이썬에서 함수를 정의하는 키워드는?

① function  
② def  
③ define  
④ func  

---

**문제 2.** 다음 코드의 실행 결과는?

```python
def greet():
    print("Hello!")

greet()
greet()
```

① Hello! (1번)  
② Hello! (2번, 각각 줄바꿈)  
③ greet greet  
④ 오류 발생  

---

**문제 3.** 함수를 정의만 하고 호출하지 않으면 어떻게 되는가?

```python
def say_hi():
    print("Hi!")
```

① "Hi!"가 출력된다  
② 아무 일도 일어나지 않는다  
③ 오류가 발생한다  
④ None이 출력된다  

---

**문제 4.** 다음 코드의 실행 결과는?

```python
def add(a, b):
    return a + b

result = add(3, 7)
print(result)
```

① 3  
② 7  
③ 10  
④ 오류 발생  

---

**문제 5.** `return`이 없는 함수가 반환하는 값은?

① 0  
② ""  
③ None  
④ False  

---

**문제 6.** 다음 코드의 실행 결과는?

```python
def greet(name):
    print(f"안녕, {name}!")

greet("철수")
```

① 안녕, name!  
② 안녕, 철수!  
③ 안녕, "철수"!  
④ 오류 발생  

---

**문제 7.** 다음 중 함수를 올바르게 호출한 것은?

```python
def multiply(a, b):
    return a * b
```

① multiply 3, 5  
② multiply(3, 5)  
③ call multiply(3, 5)  
④ def multiply(3, 5)  

---

### 🟡 중급

**문제 8.** 다음 코드의 실행 결과는?

```python
def make_coffee(menu="아메리카노"):
    print(f"{menu} 나왔습니다!")

make_coffee()
make_coffee("카페라떼")
```

① 아메리카노 나왔습니다! (2번)  
② 아메리카노 나왔습니다! / 카페라떼 나왔습니다!  
③ 오류 발생  
④ None 나왔습니다! / 카페라떼 나왔습니다!  

---

**문제 9.** 다음 코드의 실행 결과는?

```python
def show_sum(a, b):
    print(a + b)

result = show_sum(10, 20)
print(result)
```

① 30 (줄바꿈) 30  
② 30 (줄바꿈) None  
③ 30만 출력  
④ 오류 발생  

---

**문제 10.** 다음 중 올바른 함수 정의는?

① `def func(a, b=10, c):`  
② `def func(a, b, c=10):`  
③ `def func(a=10, b, c):`  
④ `def func(a=10, b=20, c):`  

---

**문제 11.** 다음 코드의 실행 결과는?

```python
def calculate(price, tax_rate=10):
    tax = price * tax_rate / 100
    return price + tax

print(calculate(1000))
print(calculate(1000, 20))
```

① 1100.0 / 1200.0  
② 1010.0 / 1020.0  
③ 1000 / 1000  
④ 오류 발생  

---

**문제 12.** 키워드 인자에 대한 설명으로 올바른 것은?

```python
def info(name, age):
    print(f"{name}, {age}")

info(age=25, name="철수")
```

① 위치 인자보다 먼저 와야 한다  
② 매개변수 이름으로 값을 전달한다  
③ 기본값이 있는 매개변수만 사용 가능하다  
④ 순서를 바꿀 수 없다  

---

### 🔴 고급

**문제 13.** 다음 코드의 실행 결과는?

```python
def calculate_discount(price, rate):
    return price * (1 - rate / 100)

original = 50000
final = calculate_discount(original, 20)
print(f"{final:,.0f}원")
```

① 50,000원  
② 40,000원  
③ 10,000원  
④ 오류 발생  

---

**문제 14.** 다음 코드의 실행 결과는?

```python
def get_ticket_price(age, is_morning=False):
    price = 14000
    if age <= 12:
        price = 9000
    if is_morning:
        price -= 2000
    return price

print(get_ticket_price(10, True))
```

① 14000  
② 12000  
③ 9000  
④ 7000  

---

**문제 15.** 다음 코드의 실행 결과는?

```python
def outer():
    x = 10
    print(x)

x = 20
outer()
print(x)
```

① 10 / 10  
② 20 / 20  
③ 10 / 20  
④ 20 / 10  

---

## 📝 주관식 문제

---

### 🟢 초급

**문제 16.** 함수의 매개변수(parameter)와 인자(argument)의 차이를 설명하시오.

---

**문제 17.** 다음 코드의 실행 결과를 쓰시오.

```python
def repeat(text, count):
    for i in range(count):
        print(text)

repeat("파이썬", 3)
```

---

**문제 18.** `return`과 `print()`의 차이점을 설명하고, 각각 언제 사용하는 것이 적합한지 설명하시오.

---

### 🟡 중급

**문제 19.** 다음 코드의 실행 결과를 쓰고, 기본값 매개변수의 동작을 설명하시오.

```python
def power(base, exponent=2):
    return base ** exponent

print(power(3))
print(power(2, 10))
print(power(5, 3))
```

---

**문제 20.** 다음 코드의 실행 결과를 쓰고, 위치 인자와 키워드 인자의 차이를 설명하시오.

```python
def order(item, quantity, unit="개"):
    print(f"{item} {quantity}{unit}")

order("사과", 3)
order("우유", 2, "팩")
order(quantity=5, item="계란", unit="판")
```

---

### 🔴 고급

**문제 21.** 다음 코드의 실행 결과를 쓰고, 여러 함수가 연쇄적으로 호출되는 과정을 설명하시오.

```python
def get_grade(avg):
    if avg >= 90: return "A"
    elif avg >= 80: return "B"
    elif avg >= 70: return "C"
    else: return "F"

def get_average(kor, eng, math):
    return (kor + eng + math) / 3

def report(name, kor, eng, math):
    avg = get_average(kor, eng, math)
    grade = get_grade(avg)
    return f"{name}: 평균 {avg:.1f}, 등급 {grade}"

print(report("홍길동", 85, 92, 78))
```

---

## 💻 실습형 문제

---

### 🟢 초급

**문제 22.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 두 수를 받아 **큰 수**를 반환하는 함수 `get_max(a, b)`를 작성하고, 테스트하시오.

출력 예시:

```
get_max(10, 20) = 20
get_max(7, 3) = 7
get_max(5, 5) = 5
```

---

**문제 23.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 이름과 인사말을 매개변수로 받는 함수 `greet(name, message="안녕하세요")`를 작성하시오. 인사말의 기본값은 `"안녕하세요"`입니다.

출력 예시:

```
철수님, 안녕하세요!
영희님, 반갑습니다!
```

---

**문제 24.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 원의 넓이를 계산하여 반환하는 함수 `circle_area(radius)`를 작성하시오. (π = 3.14)

출력 예시:

```
반지름 5의 원 넓이: 78.5
반지름 10의 원 넓이: 314.0
```

---

### 🟡 중급

**문제 25.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 세 가지 함수를 작성하여 성적 처리 프로그램을 만드시오.
> - `calc_total(kor, eng, math)` : 총점 반환
> - `calc_average(total)` : 평균 반환 (3과목)
> - `get_grade(avg)` : 등급 반환 (A/B/C/D/F)
>
> 사용자로부터 세 과목 점수를 `input()`으로 입력받아 결과를 출력하시오.

출력 예시 (국어: 85, 영어: 92, 수학: 78):

```
=== 성적표 ===
국어: 85점
영어: 92점
수학: 78점
--------------
총점: 255점
평균: 85.0점
등급: B
```

---

### 🔴 고급

**문제 26.** 다음 요구사항에 맞는 프로그램을 작성하시오.

> 상품 가격 계산 시스템을 함수로 구현하시오.
> - `apply_discount(price, rate=10)` : 할인 적용 후 가격 반환 (기본 10%)
> - `add_tax(price, tax_rate=10)` : 세금 추가 후 가격 반환 (기본 10%)
> - `calc_final_price(price, discount=10, tax=10)` : 할인 → 세금 순서로 적용한 최종 가격 반환
>
> 상품 가격 50,000원에 대해 아래 세 가지 경우를 출력하시오.

출력 예시:

```
=== 가격 계산 ===
원가: 50,000원

[기본 할인 10% + 세금 10%]
최종가: 49,500원

[할인 20% + 세금 10%]
최종가: 44,000원

[할인 없음 + 세금 5%]
최종가: 52,500원
```

---
---

# 🔑 정답 및 해설

---

## 📝 객관식 정답

---

### 🟢 초급

**문제 1. 정답: ② def**

파이썬에서 함수는 `def` 키워드로 정의합니다. `def 함수이름():` 형식입니다. `function`은 JavaScript에서 사용합니다.

---

**문제 2. 정답: ② Hello! (2번, 각각 줄바꿈)**

`greet()`를 2번 호출하므로 `"Hello!"`가 2번 출력됩니다. 함수는 호출할 때마다 내부 코드가 실행됩니다.

---

**문제 3. 정답: ② 아무 일도 일어나지 않는다**

함수는 정의(def)만 해서는 실행되지 않습니다. 반드시 `say_hi()`로 호출해야 내부 코드가 실행됩니다.

---

**문제 4. 정답: ③ 10**

`add(3, 7)`은 `3 + 7 = 10`을 반환하고, `result`에 10이 저장되어 출력됩니다.

---

**문제 5. 정답: ③ None**

`return`이 없는 함수는 자동으로 `None`을 반환합니다. `None`은 "아무 값도 없음"을 의미합니다.

---

**문제 6. 정답: ② 안녕, 철수!**

매개변수 `name`에 인자 `"철수"`가 전달됩니다. f-string에서 `{name}`이 `"철수"`로 치환됩니다.

---

**문제 7. 정답: ② multiply(3, 5)**

함수 호출은 `함수이름(인자1, 인자2)` 형식입니다. 괄호 안에 인자를 넣어야 합니다.

---

### 🟡 중급

**문제 8. 정답: ② 아메리카노 나왔습니다! / 카페라떼 나왔습니다!**

첫 번째 호출에서는 인자가 없으므로 기본값 `"아메리카노"`가 사용됩니다. 두 번째 호출에서는 `"카페라떼"`가 전달되어 기본값을 대체합니다.

---

**문제 9. 정답: ② 30 (줄바꿈) None**

`show_sum`은 `print()`로 30을 **출력**하지만, `return`이 없으므로 `None`을 반환합니다. `result`에 `None`이 저장되어 두 번째 `print`에서 `None`이 출력됩니다.

---

**문제 10. 정답: ② `def func(a, b, c=10):`**

기본값이 있는 매개변수는 기본값이 없는 매개변수 **뒤에** 와야 합니다. ①③④는 기본값 없는 매개변수가 기본값 있는 매개변수 뒤에 와서 오류입니다.

---

**문제 11. 정답: ① 1100.0 / 1200.0**

첫 번째: `tax_rate` 기본값 10 사용 → `1000 × 10/100 = 100` → `1000 + 100 = 1100.0`. 두 번째: `tax_rate=20` → `1000 × 20/100 = 200` → `1000 + 200 = 1200.0`.

---

**문제 12. 정답: ② 매개변수 이름으로 값을 전달한다**

키워드 인자는 `매개변수이름=값` 형태로 전달하며, 순서에 관계없이 사용 가능합니다. 단, 위치 인자 뒤에 와야 합니다.

---

### 🔴 고급

**문제 13. 정답: ② 40,000원**

`50000 × (1 - 20/100) = 50000 × 0.8 = 40000.0`. `{:,.0f}` 포매팅으로 `40,000`이 됩니다.

---

**문제 14. 정답: ④ 7000**

`age=10` → 12세 이하이므로 `price = 9000`. `is_morning=True` → `9000 - 2000 = 7000`. 두 할인이 순차적으로 적용됩니다.

---

**문제 15. 정답: ③ 10 / 20**

`outer()` 내부의 `x = 10`은 지역 변수이므로 10이 출력됩니다. 함수 밖의 `x = 20`은 전역 변수로, `print(x)`에서 20이 출력됩니다. 두 변수는 이름만 같을 뿐 서로 다른 변수입니다.

---

## 📝 주관식 정답

---

### 🟢 초급

**문제 16. 모범답안:**

**매개변수(parameter):** 함수를 **정의**할 때 괄호 안에 쓰는 변수 이름입니다. 함수 내부에서 사용할 값을 받는 "자리"입니다.

**인자(argument):** 함수를 **호출**할 때 괄호 안에 넣는 실제 값입니다. 매개변수에 전달되는 구체적인 데이터입니다.

```python
def greet(name):    # name은 매개변수
    print(name)

greet("철수")       # "철수"는 인자
```

---

**문제 17. 모범답안:**

```
파이썬
파이썬
파이썬
```

`repeat("파이썬", 3)`은 `text="파이썬"`, `count=3`으로 전달됩니다. `range(3)`으로 3번 반복하여 `"파이썬"`을 3줄 출력합니다.

---

**문제 18. 모범답안:**

**`return`:** 함수가 값을 **반환**합니다. 반환된 값은 변수에 저장하거나 다른 연산에 사용할 수 있습니다. 값을 이후 계산에 활용해야 할 때 사용합니다.

**`print()`:** 화면에 값을 **출력**만 합니다. 값을 반환하지 않으므로(None 반환), 결과를 다른 곳에서 재사용할 수 없습니다. 사용자에게 결과를 보여주기만 할 때 사용합니다.

```python
def get_sum(a, b):
    return a + b        # 값을 반환 → 재사용 가능

result = get_sum(10, 20)
print(result * 2)        # 60 (반환값 활용)
```

---

### 🟡 중급

**문제 19. 모범답안:**

실행 결과:

```
9
1024
125
```

**기본값 매개변수 동작:**
- `power(3)` → `exponent` 기본값 2 사용 → `3 ** 2 = 9`
- `power(2, 10)` → `exponent`에 10 전달 (기본값 무시) → `2 ** 10 = 1024`
- `power(5, 3)` → `exponent`에 3 전달 → `5 ** 3 = 125`

인자를 전달하면 기본값이 대체되고, 전달하지 않으면 기본값이 사용됩니다.

---

**문제 20. 모범답안:**

실행 결과:

```
사과 3개
우유 2팩
계란 5판
```

**위치 인자:** 함수 정의의 매개변수 **순서대로** 값이 전달됩니다.  
- `order("사과", 3)` → `item="사과"`, `quantity=3`, `unit="개"`(기본값)

**키워드 인자:** `매개변수이름=값` 형태로 **순서에 관계없이** 전달됩니다.  
- `order(quantity=5, item="계란", unit="판")` → 순서가 달라도 이름으로 매칭됩니다.

규칙: 위치 인자가 키워드 인자보다 먼저 와야 합니다.

---

### 🔴 고급

**문제 21. 모범답안:**

실행 결과:

```
홍길동: 평균 85.0, 등급 B
```

**함수 호출 과정:**
1. `report("홍길동", 85, 92, 78)` 호출
2. 내부에서 `get_average(85, 92, 78)` 호출 → `(85+92+78)/3 = 255/3 = 85.0` 반환
3. `avg = 85.0`으로 `get_grade(85.0)` 호출 → `85.0 >= 80` → `"B"` 반환
4. `grade = "B"`로 f-string 결합 → `"홍길동: 평균 85.0, 등급 B"` 반환
5. `print()`로 최종 출력

이처럼 함수가 다른 함수의 결과를 사용하는 것을 **함수의 연쇄 호출**이라 합니다.

---

## 💻 실습형 정답

---

### 🟢 초급

**문제 22. 모범답안:**

```python
def get_max(a, b):
    if a >= b:
        return a
    else:
        return b

print(f"get_max(10, 20) = {get_max(10, 20)}")
print(f"get_max(7, 3) = {get_max(7, 3)}")
print(f"get_max(5, 5) = {get_max(5, 5)}")
```

핵심: `if-else`로 비교 후 큰 값을 `return`합니다.

---

**문제 23. 모범답안:**

```python
def greet(name, message="안녕하세요"):
    print(f"{name}님, {message}!")

greet("철수")
greet("영희", "반갑습니다")
```

핵심: `message`에 기본값 `"안녕하세요"`를 지정하여, 인자 없이 호출하면 기본 인사가 사용됩니다.

---

**문제 24. 모범답안:**

```python
def circle_area(radius):
    return 3.14 * radius ** 2

print(f"반지름 5의 원 넓이: {circle_area(5)}")
print(f"반지름 10의 원 넓이: {circle_area(10)}")
```

핵심: 원 넓이 공식 `π × r²`를 함수로 구현합니다. `3.14 × 5² = 78.5`, `3.14 × 10² = 314.0`.

---

### 🟡 중급

**문제 25. 모범답안:**

```python
def calc_total(kor, eng, math):
    return kor + eng + math

def calc_average(total):
    return total / 3

def get_grade(avg):
    if avg >= 90: return "A"
    elif avg >= 80: return "B"
    elif avg >= 70: return "C"
    elif avg >= 60: return "D"
    else: return "F"

# 입력
kor = int(input("국어: "))
eng = int(input("영어: "))
math = int(input("수학: "))

# 계산
total = calc_total(kor, eng, math)
avg = calc_average(total)
grade = get_grade(avg)

# 출력
print(f"\n=== 성적표 ===")
print(f"국어: {kor}점")
print(f"영어: {eng}점")
print(f"수학: {math}점")
print(f"--------------")
print(f"총점: {total}점")
print(f"평균: {avg:.1f}점")
print(f"등급: {grade}")
```

핵심 포인트:
- 각 기능을 독립 함수로 분리하여 재사용성을 높입니다.
- 한 함수의 반환값을 다른 함수의 인자로 연결합니다.

---

### 🔴 고급

**문제 26. 모범답안:**

```python
def apply_discount(price, rate=10):
    return price * (1 - rate / 100)

def add_tax(price, tax_rate=10):
    return price * (1 + tax_rate / 100)

def calc_final_price(price, discount=10, tax=10):
    discounted = apply_discount(price, discount)
    final = add_tax(discounted, tax)
    return final

# 출력
original = 50000
print(f"=== 가격 계산 ===")
print(f"원가: {original:,}원")

# 경우 1: 기본
price1 = calc_final_price(original)
print(f"\n[기본 할인 10% + 세금 10%]")
print(f"최종가: {price1:,.0f}원")

# 경우 2: 할인 20%
price2 = calc_final_price(original, discount=20)
print(f"\n[할인 20% + 세금 10%]")
print(f"최종가: {price2:,.0f}원")

# 경우 3: 할인 없음 + 세금 5%
price3 = calc_final_price(original, discount=0, tax=5)
print(f"\n[할인 없음 + 세금 5%]")
print(f"최종가: {price3:,.0f}원")
```

핵심 포인트:
- `calc_final_price`가 `apply_discount`와 `add_tax`를 순서대로 호출합니다.
- 기본값 매개변수로 유연한 호출이 가능합니다.
- 검증: 50000 × 0.9 × 1.1 = 49500, 50000 × 0.8 × 1.1 = 44000, 50000 × 1.0 × 1.05 = 52500

---

조정현 교수 (peterchokr@gmail.com)  
영남이공대학교
