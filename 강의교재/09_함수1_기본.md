# 9장 함수 1 (기본)

---

## 📚 **학습 목표 (Learning Objectives)**

이번 장을 마치면 여러분은 함수를 정의하고 호출할 수 있으며, 매개변수와 반환값을 활용하여 재사용 가능한 코드를 작성할 수 있습니다. 함수는 프로그램을 체계적으로 구성하는 핵심 도구입니다.

---

## 1️⃣ **함수란 무엇인가?**

함수는 특정 작업을 수행하는 코드의 묶음입니다. 마치 요리 레시피처럼, 한 번 만들어두면 필요할 때마다 반복해서 사용할 수 있습니다.

```python
# 함수 없이 반복되는 코드
print("=" * 40)
print("환영합니다!")
print("=" * 40)

# ... 다른 코드 ...

print("=" * 40)
print("환영합니다!")
print("=" * 40)

# ... 또 다른 곳에서 ...

print("=" * 40)
print("환영합니다!")
print("=" * 40)
```

```python
# 함수로 만들면 간단!
def show_welcome():
    print("=" * 40)
    print("환영합니다!")
    print("=" * 40)

# 필요할 때마다 호출
show_welcome()
# ... 다른 코드 ...
show_welcome()
# ... 또 다른 곳에서 ...
show_welcome()
```

```
함수의 개념

  재료(입력) → [ 함수 ] → 결과(출력)
                 ↑
              레시피(코드)
```

---

## 2️⃣ **함수 정의와 호출**

### **기본 구조**

```python
# 함수 정의
def 함수이름():
    실행할 코드
    실행할 코드

# 함수 호출
함수이름()
```

### **간단한 예제**

```python
# 인사 함수
def greet():
    print("안녕하세요!")
    print("반갑습니다!")

# 함수 호출
greet()
greet()

# 출력:
# 안녕하세요!
# 반갑습니다!
# 안녕하세요!
# 반갑습니다!
```

⚠️ **중요**: 함수는 정의만 해서는 실행되지 않습니다. 반드시 호출해야 합니다!

### **예제 1: 메뉴판 출력**

카페 메뉴판을 출력하는 함수입니다.

```python
# 카페 메뉴 출력 함수
def show_menu():
    print("=" * 40)
    print("☕ CAFE MENU")
    print("=" * 40)
    print("1. 아메리카노    4,500원")
    print("2. 카페라떼      5,000원")
    print("3. 카푸치노      5,500원")
    print("4. 바닐라라떼    6,000원")
    print("=" * 40)

# 프로그램 시작
print("🏪 카페 주문 시스템\n")

# 메뉴 보여주기
show_menu()

choice = input("\n메뉴 번호를 선택하세요: ")
print(f"선택하신 메뉴: {choice}번")

# 다시 메뉴 보여주기
print("\n메뉴를 다시 확인하시겠습니까? (Y/N)")
answer = input()

if answer == "Y" or answer == "y":
    show_menu()
```

---

## 3️⃣ **매개변수가 있는 함수**

함수에 값을 전달하여 다양한 결과를 만들 수 있습니다.

```python
# 매개변수가 있는 함수
def greet_person(name):
    print(f"안녕하세요, {name}님!")

# 함수 호출
greet_person("철수")  # 안녕하세요, 철수님!
greet_person("영희")  # 안녕하세요, 영희님!
```

### **여러 개의 매개변수**

```python
# 두 개의 매개변수
def introduce(name, age):
    print(f"이름: {name}")
    print(f"나이: {age}세")

introduce("김철수", 20)
introduce("박영희", 25)
```

### **예제 2: 배달비 계산**

주문 금액에 따라 배달비를 계산하는 함수입니다.

```python
# 배달비 계산 함수
def calculate_delivery(order_amount):
    print(f"\n주문 금액: {order_amount:,}원")
  
    if order_amount >= 20000:
        delivery_fee = 0
        print("배달비: 무료 🎉")
    else:
        delivery_fee = 3000
        shortage = 20000 - order_amount
        print(f"배달비: {delivery_fee:,}원")
        print(f"💡 {shortage:,}원 더 주문하시면 무료 배달!")
  
    total = order_amount + delivery_fee
    print(f"총 결제금액: {total:,}원")
    print("=" * 40)

# 프로그램 시작
print("🍕" + "=" * 38 + "🍕")
print("   배달 주문 시스템")
print("🍕" + "=" * 38 + "🍕")

# 첫 번째 주문
print("\n[ 주문 1 ]")
calculate_delivery(15000)

# 두 번째 주문
print("\n[ 주문 2 ]")
calculate_delivery(25000)

# 세 번째 주문
print("\n[ 주문 3 ]")
amount = int(input("\n주문 금액을 입력하세요: "))
calculate_delivery(amount)
```

---

## 4️⃣ **반환값 (return)**

함수가 결과를 돌려주려면 `return`을 사용합니다.

```python
# 반환값이 있는 함수
def add(a, b):
    result = a + b
    return result

# 반환값 받기
sum_value = add(10, 20)
print(sum_value)  # 30

# 직접 사용
print(add(5, 7))  # 12
```

```
return의 역할

   함수 호출        함수 실행        값 반환
      │               │              │
      ▼               ▼              ▼
  add(10, 20) → [계산: 30] → return 30
      │                              │
      └──────────── 30 ──────────────┘
```

### **return과 print의 차이**

```python
# print만 하는 함수
def show_sum(a, b):
    print(a + b)

# return하는 함수
def get_sum(a, b):
    return a + b

# 차이점
show_sum(10, 20)  # 30 출력, 하지만 값을 사용할 수 없음
# result = show_sum(10, 20)  # result는 None

result = get_sum(10, 20)  # 값을 받아서 사용 가능
print(result * 2)  # 60
```

### **예제 3: 할인 가격 계산**

할인된 가격을 계산하여 반환하는 함수입니다.

```python
# 할인 계산 함수
def calculate_discount(price, discount_rate):
    discount_amount = price * discount_rate / 100
    final_price = price - discount_amount
    return final_price

# 프로그램 시작
print("🛍️" + "=" * 38 + "🛍️")
print("   쇼핑몰 할인 계산")
print("🛍️" + "=" * 38 + "🛍️")

# 상품 정보
product1 = "노트북"
price1 = 1200000

product2 = "마우스"
price2 = 30000

# 할인 적용
discount1 = calculate_discount(price1, 20)  # 20% 할인
discount2 = calculate_discount(price2, 10)  # 10% 할인

# 결과 출력
print("\n" + "=" * 40)
print("할인 적용 결과")
print("=" * 40)
print(f"{product1}")
print(f"  정가: {price1:,}원")
print(f"  할인가: {discount1:,.0f}원 (20% 할인)")

print(f"\n{product2}")
print(f"  정가: {price2:,}원")
print(f"  할인가: {discount2:,.0f}원 (10% 할인)")

# 총 결제 금액
total = discount1 + discount2
print("\n" + "-" * 40)
print(f"총 결제금액: {total:,.0f}원")
print("=" * 40)
```

---

## 5️⃣ **기본값 매개변수**

매개변수에 기본값을 지정하면, 인자를 전달하지 않아도 기본값이 사용됩니다.

```python
# 기본값이 있는 함수
def make_coffee(coffee_type="아메리카노", size="Medium"):
    print(f"{size} {coffee_type} 나왔습니다!")

# 다양한 호출 방법
make_coffee()                      # Medium 아메리카노
make_coffee("카페라떼")            # Medium 카페라떼
make_coffee("카푸치노", "Large")   # Large 카푸치노
```

⚠️ **규칙**: 기본값이 있는 매개변수는 기본값이 없는 매개변수 뒤에 와야 합니다.

```python
# 올바른 예
def func(a, b, c=10, d=20):  # OK

# 잘못된 예
# def func(a, b=10, c, d=20):  # 오류!
```

### **예제 4: 영화 티켓 계산**

나이에 따른 티켓 가격을 계산하는 함수입니다.

```python
# 영화 티켓 가격 계산 함수
def calculate_ticket(age, is_morning=False):
    base_price = 14000
  
    # 나이별 할인
    if age <= 12:
        price = base_price - 5000
        discount_type = "어린이 할인"
    elif age >= 65:
        price = base_price - 4000
        discount_type = "경로 우대"
    else:
        price = base_price
        discount_type = "정상가"
  
    # 조조 할인 (추가)
    if is_morning:
        price = price - 2000
        discount_type = discount_type + " + 조조할인"
  
    return price, discount_type

# 프로그램 시작
print("🎬" + "=" * 38 + "🎬")
print("   영화관 티켓 계산기")
print("🎬" + "=" * 38 + "🎬")

customer_age = int(input("\n나이: "))
time_input = input("조조 상영인가요? (Y/N): ")
is_morning_show = (time_input == "Y" or time_input == "y")

# 가격 계산
final_price, discount_info = calculate_ticket(customer_age, is_morning_show)

# 결과 출력
print("\n" + "=" * 40)
print("티켓 정보")
print("=" * 40)
print(f"기본 가격: 14,000원")
print(f"할인 유형: {discount_info}")
print("-" * 40)
print(f"최종 가격: {final_price:,}원")
print("=" * 40)
```

---

## 6️⃣ **위치 인자 vs 키워드 인자**

함수를 호출할 때 인자를 전달하는 두 가지 방법이 있습니다.

```python
def order_pizza(size, topping, quantity):
    print(f"{size} 사이즈 {topping} 피자 {quantity}판")

# 위치 인자 (순서대로)
order_pizza("Large", "페퍼로니", 2)

# 키워드 인자 (이름으로)
order_pizza(topping="치즈", size="Medium", quantity=1)

# 혼합 (위치 인자가 먼저!)
order_pizza("Small", quantity=3, topping="불고기")
```

### **예제 5: 학생 정보 등록**

학생 정보를 등록하는 함수입니다.

```python
# 학생 정보 등록 함수
def register_student(name, student_id, major, grade=1):
    print("\n" + "=" * 40)
    print("학생 정보 등록 완료")
    print("=" * 40)
    print(f"이름: {name}")
    print(f"학번: {student_id}")
    print(f"전공: {major}")
    print(f"학년: {grade}학년")
    print("=" * 40)

# 프로그램 시작
print("🎓" + "=" * 38 + "🎓")
print("   학생 정보 등록 시스템")
print("🎓" + "=" * 38 + "🎓")

# 다양한 방법으로 등록
print("\n[ 학생 1 - 위치 인자 ]")
register_student("김철수", "2024001", "컴퓨터공학", 1)

print("\n[ 학생 2 - 키워드 인자 ]")
register_student(
    name="박영희",
    student_id="2024002",
    major="경영학",
    grade=2
)

print("\n[ 학생 3 - 기본값 사용 ]")
register_student("이민수", "2024003", "AI소프트웨어")

print("\n[ 학생 4 - 혼합 ]")
register_student("최지은", "2024004", major="디자인", grade=3)
```

---

## 7️⃣ **함수 활용 실전**

### **예제 6: 성적 처리 시스템**

학생 성적을 처리하는 여러 함수를 만드는 프로그램입니다.

```python
# 성적 처리 함수들

# 평균 계산
def calculate_average(korean, english, math):
    total = korean + english + math
    average = total / 3
    return average

# 등급 판정
def get_grade(average):
    if average >= 90:
        return "A"
    elif average >= 80:
        return "B"
    elif average >= 70:
        return "C"
    elif average >= 60:
        return "D"
    else:
        return "F"

# 성적표 출력
def print_report(name, kor, eng, mat):
    print("\n" + "=" * 40)
    print(f"{name}님의 성적표")
    print("=" * 40)
    print(f"국어: {kor}점")
    print(f"영어: {eng}점")
    print(f"수학: {mat}점")
    print("-" * 40)
  
    avg = calculate_average(kor, eng, mat)
    grade = get_grade(avg)
  
    print(f"평균: {avg:.1f}점")
    print(f"등급: {grade}")
    print("=" * 40)

# 프로그램 시작
print("📝" + "=" * 38 + "📝")
print("   성적 처리 시스템")
print("📝" + "=" * 38 + "📝")

student_name = input("\n학생 이름: ")
score_kor = int(input("국어 점수: "))
score_eng = int(input("영어 점수: "))
score_mat = int(input("수학 점수: "))

# 성적표 출력
print_report(student_name, score_kor, score_eng, score_mat)
```

---

## 📝 **핵심 개념 정리**

함수는 `def` 키워드로 정의하며, 특정 작업을 수행하는 코드 블록입니다. 함수는 정의만 해서는 실행되지 않고, 반드시 호출해야 합니다.

매개변수를 통해 함수에 값을 전달할 수 있으며, 여러 개의 매개변수를 사용할 수 있습니다. `return` 문으로 함수의 결과를 반환할 수 있습니다.

기본값 매개변수를 사용하면 인자를 전달하지 않았을 때 사용할 기본값을 지정할 수 있습니다. 기본값이 있는 매개변수는 기본값이 없는 매개변수 뒤에 와야 합니다.

함수 호출 시 위치 인자와 키워드 인자를 사용할 수 있으며, 위치 인자가 키워드 인자보다 먼저 와야 합니다. 함수를 잘 활용하면 코드의 재사용성과 가독성이 크게 향상됩니다.

---

## 💡 **실습 과제**

### **과제 1: 온도 변환 함수**

섭씨를 화씨로, 화씨를 섭씨로 변환하는 함수를 작성하세요.

```python
# 힌트
def celsius_to_fahrenheit(celsius):
    fahrenheit = celsius * 1.8 + 32
    return fahrenheit

def fahrenheit_to_celsius(fahrenheit):
    celsius = (fahrenheit - 32) / 1.8
    return celsius

# 테스트
print(celsius_to_fahrenheit(0))    # 32.0
print(fahrenheit_to_celsius(32))   # 0.0
```

### **과제 2: BMI 계산 함수**

체중과 키를 받아 BMI를 계산하고 판정하는 함수를 작성하세요.

```python
# 힌트
def calculate_bmi(weight, height_cm):
    height_m = height_cm / 100
    bmi = weight / (height_m ** 2)
    return bmi

def get_bmi_status(bmi):
    if bmi < 18.5:
        return "저체중"
    elif bmi < 23:
        return "정상"
    elif bmi < 25:
        return "과체중"
    else:
        return "비만"

# 테스트 및 결과 출력
```

---

## ✅ **퀴즈**

### **[초급] 1번**

함수를 정의하는 키워드는?

```python
1. function
2. def
3. define
4. func
```

### **[중급] 2번**

다음 코드의 출력 결과는?

```python
def add(a, b):
    return a + b

result = add(5, 3)
print(result)
```

1. 5
2. 3
3. 8
4. 오류 발생

### **[중급] 3번**

다음 코드의 출력 결과는?

```python
def greet(name="Guest"):
    print(f"Hello, {name}!")

greet()
```

1. Hello, !
2. Hello, Guest!
3. Hello, name!
4. 오류 발생

### **[고급] 4번**

다음 중 올바른 함수 정의는?

```python
1. def func(a, b=10, c):
2. def func(a, b, c=10):
3. def func(a=10, b, c):
4. def func(a=10, b=20, c):
```

1. 1번과 2번
2. 2번
3. 1번과 3번
4. 모두 올바름

### **[고급] 5번**

다음 코드의 실행 결과는?

```python
def test():
    x = 10
    return x * 2

result = test()
print(result)
```

1. 10
2. 20
3. x
4. 오류 발생

---

## 🔑 **퀴즈 정답 및 해설**

**1번 정답: 2**
파이썬에서 함수는 `def` 키워드로 정의합니다.

**2번 정답: 3**
`add(5, 3)`은 5 + 3 = 8을 반환하고, 이 값이 result에 저장되어 출력됩니다.

**3번 정답: 2**
인자를 전달하지 않으면 기본값 "Guest"가 사용되므로 "Hello, Guest!"가 출력됩니다.

**4번 정답: 2**
기본값이 있는 매개변수는 기본값이 없는 매개변수 뒤에 와야 합니다. 2번이 올바릅니다.

**5번 정답: 2**
함수 내부에서 x에 10을 할당하고 10 * 2 = 20을 반환하여 출력됩니다.

---

## 🎯 **다음 장 예고**

다음 장에서는 함수의 고급 기능에 대해 배웁니다. 람다 함수, 재귀 함수, 그리고 스코프(지역/전역 변수) 등을 학습하여 함수를 더욱 효과적으로 활용할 수 있게 됩니다!

---

수고했습니다.   
조정현 교수([peterchokr@gmail.com](mailto:peterchokr@gmail.com)) 영남이공대학교

이 수업자료는 Claude와 Gemini를 이용하여 제작되었습니다.
