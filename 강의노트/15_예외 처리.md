# 15장 예외 처리

---

## 📚 **학습 목표 (Learning Objectives)**

이번 장을 마치면 여러분은 프로그램 실행 중 발생하는 오류를 감지하고 처리할 수 있습니다. 예외 처리를 통해 프로그램이 예상치 못한 상황에서도 중단되지 않고 안정적으로 동작하는 견고한 프로그램을 만들 수 있습니다.

---

## 1️⃣ **예외란 무엇인가?**

프로그램을 만들다 보면 예상치 못한 상황들이 발생합니다. 사용자가 숫자를 입력해야 하는데 문자를 입력한다거나, 0으로 나누기를 시도한다거나, 존재하지 않는 파일을 열려고 할 때 등입니다.

이런 상황에서 파이썬은 **예외(Exception)**를 발생시키고, 아무 조치도 취하지 않으면 프로그램이 그 자리에서 멈춰버립니다. 사용자 입장에서는 매우 당황스러운 경험이죠.

### **예외가 발생하는 대표적인 상황들**

**1. 타입 오류 (ValueError)**

```python
# 숫자를 입력받으려고 했는데...
age = int(input("나이: "))
# 사용자가 "스물다섯"이라고 입력하면?
# ValueError: invalid literal for int() with base 10: '스물다섯'
# 프로그램 중단! ❌
```

**2. 0으로 나누기 (ZeroDivisionError)**

```python
# 평균을 계산하려고 했는데...
total = 100
count = 0
average = total / count
# ZeroDivisionError: division by zero
# 프로그램 중단! ❌
```

**3. 파일이 없음 (FileNotFoundError)**

```python
# 파일을 열려고 했는데...
file = open("없는파일.txt", "r")
# FileNotFoundError: No such file or directory: '없는파일.txt'
# 프로그램 중단! ❌
```

**4. 리스트 인덱스 오류 (IndexError)**

```python
# 리스트의 항목에 접근하려고 했는데...
fruits = ["사과", "바나나", "오렌지"]
print(fruits[10])
# IndexError: list index out of range
# 프로그램 중단! ❌
```

### **예외 처리의 필요성**

상상해보세요. 여러분이 만든 쇼핑몰 프로그램에서 사용자가 실수로 잘못된 값을 입력했는데 프로그램이 갑자기 멈춰버린다면? 사용자는 다시는 그 프로그램을 사용하지 않을 것입니다.

```
예외 처리가 없을 때 vs 있을 때

❌ 예외 처리 없음              ✅ 예외 처리 있음
┌─────────────────┐          ┌─────────────────┐
│ 나이: 스물다섯   │          │ 나이: 스물다섯   │
│ [오류 발생]     │          │ ⚠️ 숫자로 입력  │
│ 프로그램 종료   │          │ 나이: 25        │
│                 │          │ ✓ 정상 진행     │
└─────────────────┘          └─────────────────┘

사용자: 😡                   사용자: 😊
"이게 뭐야!"                 "친절하네!"
```

예외 처리를 하면:

- 프로그램이 중단되지 않고 계속 실행
- 사용자에게 친절한 오류 메시지 제공
- 문제를 수정하고 다시 시도할 기회 제공
- 전문적이고 안정적인 프로그램 구현

---

## 2️⃣ **try-except 기본 - 예외 잡기**

예외가 발생할 수 있는 코드를 안전하게 감싸는 방법이 바로 `try-except` 문입니다. 마치 안전망을 치는 것과 같습니다.

### **try-except의 작동 원리**

```python
try:
    # 위험할 수 있는 코드를 여기에
    # "이 코드를 실행해봐. 문제가 생길 수 있어."
    위험한_작업()
  
except:
    # 예외가 발생했을 때 실행할 코드
    # "문제가 생기면 이렇게 처리해."
    안전한_대응()
```

```
try-except 흐름도

    시작
     │
     ▼
┌────────┐
│  try   │ 코드 실행 시도
└───┬────┘
    │
    ├─── 정상 실행 ──→ 계속 진행
    │
    └─── 예외 발생 ──→ except 블록 실행
                         │
                         ▼
                      계속 진행
```

### **간단한 예제로 이해하기**

**예외 처리가 없는 코드:**

```python
# 위험한 코드
number = int(input("숫자를 입력하세요: "))
print(f"입력한 숫자: {number}")
print("프로그램 계속 실행...")

# 사용자가 "abc"를 입력하면?
# ValueError 발생!
# 프로그램 즉시 종료!
# "프로그램 계속 실행..."은 출력되지 않음
```

**예외 처리가 있는 코드:**

```python
# 안전한 코드
try:
    number = int(input("숫자를 입력하세요: "))
    print(f"입력한 숫자: {number}")
except:
    print("❌ 오류! 숫자가 아닙니다.")
    print("💡 숫자로 다시 입력해주세요.")

print("프로그램 계속 실행...")

# 사용자가 "abc"를 입력하면?
# except 블록이 실행됨
# 친절한 메시지 출력
# 프로그램은 계속 실행! ✅
```

**실행 결과 비교:**

```
[정상 입력 - 25]
숫자를 입력하세요: 25
입력한 숫자: 25
프로그램 계속 실행...

[잘못된 입력 - abc]
숫자를 입력하세요: abc
❌ 오류! 숫자가 아닙니다.
💡 숫자로 다시 입력해주세요.
프로그램 계속 실행...
```

프로그램이 중단되지 않고 계속 실행됩니다!

### **여러 번 시도하기 - while과 결합**

사용자가 올바른 입력을 할 때까지 계속 물어보는 것이 더 친절합니다:

```python
# 올바른 입력을 받을 때까지 반복
while True:
    try:
        age = int(input("나이를 입력하세요: "))
        print(f"✓ 입력 성공! {age}세")
        break  # 성공하면 반복 종료
    
    except:
        print("❌ 숫자만 입력해주세요.")
        print("💡 예: 25\n")
        # break가 없으므로 다시 입력받음

print("다음 단계로 진행합니다...")
```

**실행 예시:**

```
나이를 입력하세요: 스물다섯
❌ 숫자만 입력해주세요.
💡 예: 25

나이를 입력하세요: twenty
❌ 숫자만 입력해주세요.
💡 예: 25

나이를 입력하세요: 25
✓ 입력 성공! 25세
다음 단계로 진행합니다...
```

### **try-except를 언제 사용하나요?**

예외 처리는 **"문제가 생길 수 있는 곳"**에 사용합니다:

1. **사용자 입력**: 사용자는 예상 못한 값을 입력할 수 있음
2. **파일 작업**: 파일이 없거나, 권한이 없거나, 손상될 수 있음
3. **네트워크 통신**: 인터넷 연결이 끊기거나, 서버가 응답 안 할 수 있음
4. **외부 데이터**: JSON, CSV 등 데이터 형식이 잘못될 수 있음
5. **계산**: 0으로 나누기, 음수의 제곱근 등

⚠️ **주의**: 모든 코드를 try-except로 감싸면 안 됩니다! 진짜 버그를 숨길 수 있습니다. 문제가 생길 수 있는 부분만 선택적으로 사용하세요.

### **예제 1: 안전한 계산기**

0으로 나누기 오류를 처리하는 계산기입니다.

```python
# 안전한 나눗셈 계산기
print("➗" + "=" * 38 + "➗")
print("   나눗셈 계산기")
print("➗" + "=" * 38 + "➗")

while True:
    print("\n" + "=" * 40)
  
    try:
        num1 = float(input("첫 번째 숫자: "))
        num2 = float(input("두 번째 숫자: "))
    
        result = num1 / num2
    
        print("\n" + "-" * 40)
        print(f"{num1} ÷ {num2} = {result}")
        print("-" * 40)
  
    except ZeroDivisionError:
        print("\n❌ 0으로 나눌 수 없습니다!")
  
    except ValueError:
        print("\n❌ 숫자를 입력해주세요!")
  
    except:
        print("\n❌ 알 수 없는 오류가 발생했습니다!")
  
    # 계속할지 물어보기
    choice = input("\n계속하시겠습니까? (Y/N): ")
    if choice != "Y" and choice != "y":
        print("\n계산기를 종료합니다.")
        break
```

---

## 3️⃣ **다양한 예외 유형 - 정확하게 잡기**

지금까지는 `except:` 만 사용해서 모든 예외를 한꺼번에 잡았습니다. 하지만 예외 종류에 따라 다르게 처리하고 싶다면 어떻게 해야 할까요?

### **왜 예외 유형을 구분해야 할까요?**

```python
# 모든 예외를 똑같이 처리 (권장 안 함)
try:
    age = int(input("나이: "))
    result = 100 / age
except:
    print("오류가 발생했습니다!")  # 어떤 오류인지 모름!
```

사용자가 "스물"을 입력했을 때와 "0"을 입력했을 때, 오류 원인이 다릅니다. 하지만 같은 메시지만 보여주면 사용자는 무엇이 잘못됐는지 알 수 없습니다.

### **주요 예외 유형**

파이썬의 예외는 종류가 매우 다양합니다. 자주 만나는 것들을 알아봅시다:

```
📋 자주 만나는 예외들

🔢 숫자 관련
├─ ValueError        : 잘못된 값 (int("abc"))
├─ ZeroDivisionError : 0으로 나누기
└─ OverflowError     : 숫자가 너무 큼

📁 파일 관련
├─ FileNotFoundError : 파일이 없음
├─ PermissionError   : 권한 없음
└─ IsADirectoryError : 파일이 아닌 폴더

📚 자료구조 관련
├─ IndexError        : 인덱스 범위 초과
├─ KeyError          : 딕셔너리 키 없음
├─ KeyboardInterrupt : Ctrl+C로 중단
└─ TypeError         : 타입 불일치

🌐 기타
├─ AttributeError    : 속성/메서드 없음
├─ ImportError       : 모듈을 찾을 수 없음
└─ NameError         : 변수 이름이 정의 안 됨
```

### **특정 예외만 처리하기**

예외 유형을 지정하면 그 예외만 잡을 수 있습니다:

```python
# ValueError만 처리
try:
    age = int(input("나이: "))
    print(f"당신은 {age}세입니다.")
  
except ValueError:
    print("❌ 숫자를 입력해주세요!")
    print("💡 예: 25")
```

이제 `ValueError`(잘못된 값)만 잡고, 다른 오류는 프로그램을 중단시킵니다. 이것이 더 안전합니다. 예상하지 못한 오류를 숨기지 않기 때문입니다.

### **여러 예외를 다르게 처리하기**

각 예외마다 다른 메시지를 보여줄 수 있습니다:

```python
# 여러 예외를 구분해서 처리
try:
    # 사용자 입력
    numbers = [1, 2, 3, 4, 5]
    index = int(input("인덱스 (0-4): "))
  
    # 인덱스로 접근
    print(f"선택한 숫자: {numbers[index]}")
  
    # 계산
    result = 100 / index
    print(f"계산 결과: {result}")

except ValueError:
    # int() 변환 실패
    print("❌ 숫자를 입력해주세요!")
  
except IndexError:
    # 리스트 범위 벗어남
    print("❌ 0부터 4 사이의 숫자를 입력해주세요!")
    print(f"💡 사용 가능한 인덱스: 0, 1, 2, 3, 4")
  
except ZeroDivisionError:
    # 0으로 나누기
    print("❌ 0은 입력할 수 없습니다!")
    print("💡 1 이상의 숫자를 입력해주세요.")
```

**실행 예시:**

```
[경우 1: 문자 입력]
인덱스 (0-4): abc
❌ 숫자를 입력해주세요!

[경우 2: 범위 초과]
인덱스 (0-4): 10
❌ 0부터 4 사이의 숫자를 입력해주세요!
💡 사용 가능한 인덱스: 0, 1, 2, 3, 4

[경우 3: 0 입력]
인덱스 (0-4): 0
선택한 숫자: 1
❌ 0은 입력할 수 없습니다!
💡 1 이상의 숫자를 입력해주세요.

[경우 4: 정상]
인덱스 (0-4): 2
선택한 숫자: 3
계산 결과: 50.0
```

### **여러 예외를 같은 방식으로 처리하기**

때로는 여러 예외를 똑같이 처리하고 싶을 때가 있습니다. 괄호로 묶으면 됩니다:

```python
try:
    # 파일 열기 시도
    with open(filename, "r") as file:
        content = file.read()
    
except (FileNotFoundError, PermissionError):
    # 두 예외를 같은 방식으로 처리
    print("❌ 파일을 열 수 없습니다!")
    print("💡 파일 이름과 권한을 확인해주세요.")
```

### **모든 예외 잡기 (신중하게)**

특정 예외들을 처리한 후, 나머지 모든 예외를 잡고 싶다면:

```python
try:
    # 위험한 작업
    risky_operation()
  
except ValueError:
    # ValueError는 특별히 처리
    print("값 오류입니다.")
  
except FileNotFoundError:
    # FileNotFoundError도 특별히 처리
    print("파일을 찾을 수 없습니다.")
  
except:
    # 나머지 모든 예외
    print("알 수 없는 오류가 발생했습니다.")
```

⚠️ **주의**: 마지막의 `except:`는 정말 예상하지 못한 오류를 잡기 위한 것입니다. 너무 많이 사용하면 버그를 찾기 어려워집니다.

### **예외 유형 확인하는 법**

"어떤 예외가 발생하는지 어떻게 알아요?" - 좋은 질문입니다!

**방법 1: 직접 실행해보기**

```python
# 일부러 오류를 발생시켜 본다
int("abc")  # ValueError 발생!
```

**방법 2: 문서 읽기**
파이썬 함수 문서를 보면 어떤 예외가 발생할 수 있는지 나와 있습니다.

**방법 3: 먼저 모든 예외를 잡고, 예외 타입 확인**

```python
try:
    # 어떤 예외가 나올지 모르는 코드
    mysterious_function()
except Exception as e:
    print(f"예외 타입: {type(e).__name__}")
    print(f"예외 메시지: {e}")
```

이렇게 하면 어떤 예외가 발생하는지 알 수 있고, 나중에 적절한 except 블록을 추가할 수 있습니다!

### **예제 2: 파일 읽기**

파일 관련 예외를 처리하는 프로그램입니다.

```python
# 안전한 파일 읽기 프로그램
print("📄" + "=" * 38 + "📄")
print("   파일 읽기")
print("📄" + "=" * 38 + "📄")

while True:
    filename = input("\n파일 이름: ")
  
    if filename == "q" or filename == "Q":
        print("\n프로그램을 종료합니다.")
        break
  
    try:
        with open(filename, "r", encoding="utf-8") as file:
            content = file.read()
    
        print("\n" + "=" * 40)
        print(f"📖 {filename}")
        print("=" * 40)
        print(content)
        print("=" * 40)
  
    except FileNotFoundError:
        print(f"\n❌ '{filename}' 파일을 찾을 수 없습니다!")
        print("💡 파일 이름을 확인해주세요.")
  
    except PermissionError:
        print(f"\n❌ '{filename}' 파일을 읽을 권한이 없습니다!")
  
    except UnicodeDecodeError:
        print(f"\n❌ '{filename}' 파일의 인코딩이 올바르지 않습니다!")
        print("💡 텍스트 파일이 아닐 수 있습니다.")
  
    except:
        print("\n❌ 파일을 읽는 중 오류가 발생했습니다!")
```

---

## 4️⃣ **예외 메시지 받기 - 더 자세한 정보**

예외가 발생하면 파이썬은 "무엇이 잘못되었는지" 설명하는 메시지를 함께 보내줍니다. 이 메시지를 받아서 사용자에게 보여주거나, 로그에 기록할 수 있습니다.

### **as 키워드로 예외 정보 받기**

```python
try:
    number = int(input("숫자: "))
except ValueError as e:
    # e 변수에 예외 정보가 담김
    print(f"오류 발생!")
    print(f"상세 내용: {e}")
```

**실행 예시:**

```
숫자: 안녕
오류 발생!
상세 내용: invalid literal for int() with base 10: '안녕'
```

### **예외 정보로 할 수 있는 일들**

**1. 사용자에게 자세한 정보 제공**

```python
try:
    with open(filename, "r") as file:
        content = file.read()
    
except FileNotFoundError as e:
    print(f"❌ 파일을 찾을 수 없습니다!")
    print(f"💡 찾으려던 파일: {filename}")
    print(f"📋 시스템 메시지: {e}")
```

**2. 로그 파일에 기록**

```python
import datetime

try:
    risky_operation()
except Exception as e:
    # 오류를 파일에 기록
    with open("error_log.txt", "a") as log:
        timestamp = datetime.datetime.now()
        log.write(f"[{timestamp}] {type(e).__name__}: {e}\n")
  
    # 사용자에게는 간단한 메시지
    print("오류가 발생했습니다. 관리자에게 문의하세요.")
```

**3. 예외 유형 확인**

```python
try:
    something()
except Exception as e:
    error_type = type(e).__name__  # 예외 타입 이름
    error_msg = str(e)             # 예외 메시지
  
    print(f"예외 유형: {error_type}")
    print(f"메시지: {error_msg}")
```

### **실전 예제: 딕셔너리 검색**

```python
# 과일 가격 검색 프로그램 (개선된 오류 처리)
fruits = {
    "사과": 3000,
    "바나나": 2000,
    "오렌지": 4000
}

print("🍎 과일 가격표")
print("등록된 과일:", ", ".join(fruits.keys()))

fruit = input("\n검색할 과일: ")

try:
    price = fruits[fruit]
    print(f"✓ {fruit}: {price:,}원")
  
except KeyError as e:
    print(f"❌ 등록되지 않은 과일입니다!")
    print(f"💡 찾으려던 과일: {e}")  # 'KeyError의 메시지'
  
    # 비슷한 이름 추천
    similar = [f for f in fruits.keys() if fruit[0] in f]
    if similar:
        print(f"💡 혹시 이것을 찾으시나요? {', '.join(similar)}")
```

---

## 5️⃣ **finally - 항상 실행되는 코드**

`finally` 블록은 예외 발생 여부와 **관계없이 항상 실행**됩니다. 마치 "어떤 일이 있어도 반드시 해야 하는 일"을 지정하는 것입니다.

### **finally가 필요한 이유**

파일을 열었으면 닫아야 하고, 데이터베이스에 연결했으면 연결을 끊어야 하고, 자원을 빌렸으면 반납해야 합니다. 예외가 발생하든 안 하든 이런 "정리 작업"은 반드시 해야 합니다.

```
try-except-finally 실행 흐름

              시작
               │
           ┌───▼───┐
           │  try  │ 코드 실행
           └───┬───┘
               │
        ┌──────┴──────┐
     정상실행      예외발생
        │              │
        │          ┌───▼────┐
        │          │ except │ 예외 처리
        │          └───┬────┘
        │              │
        └──────┬───────┘
               │
           ┌───▼────┐
           │finally │ 항상 실행!
           └───┬────┘
               │
              종료
```

### **기본 예제**

```python
try:
    print("1. 작업 시작")
    result = 10 / 0  # 예외 발생!
    print("2. 작업 완료")  # 실행 안 됨
  
except ZeroDivisionError:
    print("3. 오류 처리")
  
finally:
    print("4. 정리 작업 (항상 실행)")
  
print("5. 프로그램 계속")
```

**실행 결과:**

```
1. 작업 시작
3. 오류 처리
4. 정리 작업 (항상 실행)
5. 프로그램 계속
```

"2. 작업 완료"는 실행되지 않았지만, "4. 정리 작업"은 실행되었습니다!

### **실전 예제: 파일 작업**

```python
# 파일 작업 with finally
file = None  # 변수 미리 선언

try:
    print("📂 파일 열기 시도...")
    file = open("data.txt", "r")
    content = file.read()
    print(f"✓ 파일 읽기 성공!")
    print(content)
  
except FileNotFoundError:
    print("❌ 파일을 찾을 수 없습니다!")
  
finally:
    # 파일이 열렸든 안 열렸든, 열려있다면 닫기
    if file:
        file.close()
        print("🔒 파일 닫기 완료")
    print("정리 작업 완료")
```

💡 **참고**: `with` 문을 사용하면 자동으로 파일을 닫아주므로 더 간편합니다. 하지만 `finally`의 개념을 이해하는 것이 중요합니다.

---

## 6️⃣ **else - 예외가 없을 때만 실행**

`else` 블록은 `try` 블록에서 **예외가 발생하지 않았을 때만** 실행됩니다.

```
try-except-else-finally 전체 흐름

    try
     │
  ┌──┴───┐
예외O   예외X
  │      │
except  else
  │      │
  └──┬───┘
     │
  finally
     │
    종료
```

### **왜 else를 사용할까요?**

"예외가 없을 때만 실행해야 하는 코드"가 있을 때 사용합니다. try 블록 안에 너무 많은 코드를 넣는 것보다, 정말 예외가 발생할 수 있는 부분만 try에 넣고, 그 다음 작업은 else에 넣는 것이 더 명확합니다.

### **기본 예제**

```python
try:
    number = int(input("숫자: "))
  
except ValueError:
    print("❌ 숫자가 아닙니다!")
  
else:
    # 예외가 없었을 때만 실행
    print(f"✓ 입력 성공: {number}")
    print(f"두 배: {number * 2}")
    print(f"제곱: {number ** 2}")
  
finally:
    # 항상 실행
    print("입력 작업 종료")
```

**실행 예시:**

```
[정상 입력]
숫자: 5
✓ 입력 성공: 5
두 배: 10
제곱: 25
입력 작업 종료

[잘못된 입력]
숫자: abc
❌ 숫자가 아닙니다!
입력 작업 종료
```

### **실전 예제: 파일 처리 완전판**

```python
filename = input("파일 이름: ")

try:
    # 파일 열기 (예외 발생 가능)
    file = open(filename, "r", encoding="utf-8")
  
except FileNotFoundError:
    print(f"❌ '{filename}' 파일이 없습니다!")
  
else:
    # 파일이 성공적으로 열렸을 때만 실행
    print(f"✓ '{filename}' 파일 열기 성공")
  
    # 파일 내용 읽기
    content = file.read()
    lines = content.split('\n')
  
    print(f"📊 통계:")
    print(f"  - 줄 수: {len(lines)}")
    print(f"  - 글자 수: {len(content)}")
  
    # 파일 닫기
    file.close()
  
finally:
    print("작업 완료")
```

### **완전한 구조 정리**

```python
try:
    # 예외가 발생할 수 있는 코드만
    dangerous_operation()
  
except ValueError:
    # ValueError 처리
    handle_value_error()
  
except FileNotFoundError:
    # FileNotFoundError 처리
    handle_file_error()
  
else:
    # 예외가 없을 때만 실행
    success_operation()
  
finally:
    # 항상 실행 (정리 작업)
    cleanup()
```

**실행 순서:**

1. `try` 실행
2. 예외 발생 → `except` 실행 → `finally` 실행
3. 예외 없음 → `else` 실행 → `finally` 실행

💡 **팁**: 모든 블록을 항상 다 쓸 필요는 없습니다. 필요한 것만 선택해서 사용하세요!

### **예제 3: 상세한 오류 메시지**

오류 정보를 자세히 보여주는 프로그램입니다.

```python
# 딕셔너리 검색 프로그램
print("🔍" + "=" * 38 + "🔍")
print("   과일 가격 검색")
print("🔍" + "=" * 38 + "🔍")

fruits = {
    "사과": 3000,
    "바나나": 2000,
    "오렌지": 4000,
    "포도": 5000,
    "딸기": 8000
}

print("\n등록된 과일:")
for fruit in fruits.keys():
    print(f"  • {fruit}")

while True:
    print("\n" + "=" * 40)
    fruit = input("과일 이름 (종료: q): ")
  
    if fruit == "q" or fruit == "Q":
        print("\n프로그램을 종료합니다.")
        break
  
    try:
        price = fruits[fruit]
        print(f"\n✓ {fruit}: {price:,}원")
  
    except KeyError as e:
        print(f"\n❌ 오류: {e}")
        print(f"'{fruit}'는 등록되지 않은 과일입니다.")
    
        # 추천
        similar = [f for f in fruits.keys() if fruit[0] in f]
        if len(similar) > 0:
            print(f"\n💡 혹시 이것을 찾으시나요?")
            for item in similar:
                print(f"  • {item}")
```

---

## 5️⃣ **finally 절**

`finally` 절은 예외 발생 여부와 관계없이 항상 실행됩니다.

```python
try:
    file = open("data.txt", "r")
    content = file.read()
    print(content)
except FileNotFoundError:
    print("파일이 없습니다!")
finally:
    # 예외 발생 여부와 관계없이 실행
    print("파일 작업 종료")
    # file.close()  # 파일이 열렸다면 닫기
```

### **예제 4: 데이터 저장**

항상 저장 상태를 확인하는 프로그램입니다.

```python
# 메모 저장 프로그램
print("💾" + "=" * 38 + "💾")
print("   메모 저장")
print("💾" + "=" * 38 + "💾")

filename = input("\n파일 이름: ")

print("\n메모 내용을 입력하세요 (종료: 빈 줄):")
print("-" * 40)

lines = []
while True:
    line = input()
    if line == "":
        break
    lines.append(line)

try:
    with open(filename, "w", encoding="utf-8") as file:
        for line in lines:
            file.write(line + "\n")
  
    print("\n✓ 저장 성공!")
    saved = True

except PermissionError:
    print("\n❌ 파일을 저장할 권한이 없습니다!")
    saved = False

except OSError as e:
    print(f"\n❌ 저장 실패: {e}")
    saved = False

finally:
    # 항상 실행
    print("\n" + "=" * 40)
    print("저장 작업 종료")
    print("=" * 40)
  
    if saved:
        print(f"파일: {filename}")
        print(f"줄 수: {len(lines)}줄")
    else:
        print("저장되지 않았습니다.")
```

---

## 6️⃣ **try-except-else**

`else` 절은 예외가 발생하지 않았을 때만 실행됩니다.

```python
try:
    number = int(input("숫자: "))
except ValueError:
    print("숫자가 아닙니다!")
else:
    # 예외가 없을 때만 실행
    print(f"입력한 숫자: {number}")
    print("정상 처리되었습니다.")
finally:
    # 항상 실행
    print("작업 완료")
```

### **예제 5: 회원가입**

입력 검증을 하는 회원가입 프로그램입니다.

```python
# 회원가입 프로그램
print("👤" + "=" * 38 + "👤")
print("   회원가입")
print("👤" + "=" * 38 + "👤")

users = {}  # {아이디: 비밀번호}

while True:
    print("\n" + "=" * 40)
    print("1. 회원가입")
    print("2. 로그인")
    print("3. 종료")
    print("=" * 40)
  
    choice = input("\n선택: ")
  
    if choice == "1":
        print("\n[ 회원가입 ]")
        user_id = input("아이디: ")
    
        try:
            # 아이디 중복 확인
            if user_id in users:
                raise ValueError("이미 사용 중인 아이디입니다")
        
            # 아이디 길이 확인
            if len(user_id) < 4:
                raise ValueError("아이디는 4자 이상이어야 합니다")
        
            password = input("비밀번호: ")
        
            # 비밀번호 길이 확인
            if len(password) < 6:
                raise ValueError("비밀번호는 6자 이상이어야 합니다")
        
            confirm = input("비밀번호 확인: ")
        
            # 비밀번호 일치 확인
            if password != confirm:
                raise ValueError("비밀번호가 일치하지 않습니다")
    
        except ValueError as e:
            print(f"\n❌ {e}")
    
        else:
            # 예외가 없을 때만 실행
            users[user_id] = password
            print(f"\n✓ 회원가입 성공! 환영합니다, {user_id}님!")
    
        finally:
            print("-" * 40)
  
    elif choice == "2":
        print("\n[ 로그인 ]")
        user_id = input("아이디: ")
        password = input("비밀번호: ")
    
        try:
            if user_id not in users:
                raise KeyError("존재하지 않는 아이디입니다")
        
            if users[user_id] != password:
                raise ValueError("비밀번호가 틀렸습니다")
    
        except (KeyError, ValueError) as e:
            print(f"\n❌ {e}")
    
        else:
            print(f"\n✓ 로그인 성공! 환영합니다, {user_id}님!")
    
        finally:
            print("-" * 40)
  
    elif choice == "3":
        print("\n프로그램을 종료합니다.")
        break
  
    else:
        print("\n잘못된 선택입니다.")
```

---

## 7️⃣ **예외 직접 발생시키기 (raise)**

지금까지는 파이썬이 자동으로 발생시키는 예외를 처리하는 방법을 배웠습니다. 하지만 때로는 우리가 직접 예외를 발생시켜야 할 때가 있습니다.

### **왜 일부러 예외를 발생시킬까요?**

프로그램의 규칙을 강제하기 위해서입니다. 예를 들어:

- 나이는 0보다 커야 한다
- 비밀번호는 6자 이상이어야 한다
- 파일 크기는 10MB를 넘으면 안 된다

이런 규칙을 어겼을 때 예외를 발생시켜서 프로그램을 안전하게 만들 수 있습니다.

### **raise 키워드 사용법**

```python
# 기본 형태
raise 예외종류("오류 메시지")

# 예시
raise ValueError("나이는 0보다 커야 합니다")
raise TypeError("문자열이 아닙니다")
raise FileNotFoundError("파일을 찾을 수 없습니다")
```

### **간단한 예제**

```python
def divide(a, b):
    """두 수를 나누는 함수"""
  
    # 0으로 나누기를 사전에 차단
    if b == 0:
        raise ValueError("0으로 나눌 수 없습니다!")
  
    return a / b

# 사용
try:
    result = divide(10, 0)
    print(result)
  
except ValueError as e:
    print(f"❌ {e}")
```

**실행 결과:**

```
❌ 0으로 나눌 수 없습니다!
```

### **입력 검증에 활용하기**

```python
def create_user(username, age):
    """사용자 생성 함수"""
  
    # 아이디 검증
    if not username:
        raise ValueError("아이디를 입력해주세요")
  
    if len(username) < 3:
        raise ValueError("아이디는 3자 이상이어야 합니다")
  
    # 나이 검증
    if not isinstance(age, int):
        raise TypeError("나이는 숫자여야 합니다")
  
    if age < 0:
        raise ValueError("나이는 0 이상이어야 합니다")
  
    if age > 150:
        raise ValueError("나이가 너무 많습니다")
  
    # 모든 검증 통과
    return {
        "username": username,
        "age": age
    }

# 사용 예시
try:
    user = create_user("ab", 25)
    print(f"✓ 사용자 생성: {user}")
  
except ValueError as e:
    print(f"❌ {e}")
  
except TypeError as e:
    print(f"❌ {e}")
```

**실행 결과:**

```
❌ 아이디는 3자 이상이어야 합니다
```

### **실전 예제: 은행 계좌 클래스**

```python
class BankAccount:
    """은행 계좌 클래스"""
  
    def __init__(self, balance=0):
        if balance < 0:
            raise ValueError("초기 잔액은 0 이상이어야 합니다")
        self.balance = balance
  
    def deposit(self, amount):
        """입금"""
        if amount <= 0:
            raise ValueError("입금액은 0보다 커야 합니다")
    
        if amount > 10000000:
            raise ValueError("1회 입금 한도는 1,000만원입니다")
    
        self.balance += amount
        return self.balance
  
    def withdraw(self, amount):
        """출금"""
        if amount <= 0:
            raise ValueError("출금액은 0보다 커야 합니다")
    
        if amount > self.balance:
            raise ValueError(f"잔액이 부족합니다 (잔액: {self.balance:,}원)")
    
        if amount > 5000000:
            raise ValueError("1회 출금 한도는 500만원입니다")
    
        self.balance -= amount
        return self.balance

# 계좌 사용
print("💳 은행 계좌 시스템\n")

try:
    # 계좌 생성
    account = BankAccount(10000)
    print(f"계좌 생성 완료 (잔액: {account.balance:,}원)\n")
  
    # 입금
    account.deposit(50000)
    print(f"✓ 50,000원 입금 (잔액: {account.balance:,}원)")
  
    # 출금
    account.withdraw(30000)
    print(f"✓ 30,000원 출금 (잔액: {account.balance:,}원)")
  
    # 잔액 초과 출금 시도
    account.withdraw(50000)
  
except ValueError as e:
    print(f"\n❌ 오류: {e}")
```

**실행 결과:**

```
💳 은행 계좌 시스템

계좌 생성 완료 (잔액: 10,000원)

✓ 50,000원 입금 (잔액: 60,000원)
✓ 30,000원 출금 (잔액: 30,000원)

❌ 오류: 잔액이 부족합니다 (잔액: 30,000원)
```

### **사용자 정의 예외 만들기**

더 명확한 예외를 만들고 싶다면 직접 예외 클래스를 만들 수 있습니다:

```python
# 사용자 정의 예외
class InvalidAgeError(Exception):
    """나이가 유효하지 않을 때 발생하는 예외"""
    pass

class InvalidPasswordError(Exception):
    """비밀번호가 유효하지 않을 때 발생하는 예외"""
    pass

def validate_age(age):
    """나이 검증"""
    if age < 0:
        raise InvalidAgeError("나이는 0 이상이어야 합니다")
  
    if age > 150:
        raise InvalidAgeError("나이가 너무 많습니다")
  
    return True

def validate_password(password):
    """비밀번호 검증"""
    if len(password) < 8:
        raise InvalidPasswordError("비밀번호는 8자 이상이어야 합니다")
  
    if not any(c.isdigit() for c in password):
        raise InvalidPasswordError("비밀번호에 숫자가 포함되어야 합니다")
  
    return True

# 사용
try:
    validate_age(200)
except InvalidAgeError as e:
    print(f"나이 오류: {e}")

try:
    validate_password("abc")
except InvalidPasswordError as e:
    print(f"비밀번호 오류: {e}")
```

### **raise를 언제 사용할까요?**

✅ **사용하면 좋은 경우:**

- 함수의 입력값 검증
- 비즈니스 규칙 강제 (나이 제한, 금액 제한 등)
- 잘못된 상태 방지 (음수 잔액, 중복 아이디 등)
- API 사용 규칙 명시

❌ **사용을 피해야 하는 경우:**

- 일반적인 제어 흐름 (if-else로 충분한 경우)
- 성능이 중요한 반복문 내부
- 예상 가능한 정상적인 경우

### **raise의 특별한 용법**

**1. 예외 다시 발생시키기**

```python
try:
    risky_operation()
except ValueError as e:
    print(f"로그 기록: {e}")
    raise  # 같은 예외를 다시 발생시킴
```

**2. 다른 예외로 변환**

```python
try:
    data = json.loads(text)
except json.JSONDecodeError:
    # JSON 오류를 더 일반적인 오류로 변환
    raise ValueError("잘못된 데이터 형식입니다")
```

raise를 잘 활용하면 더 안전하고 명확한 코드를 작성할 수 있습니다!

### **예제 6: 나이 검증**

나이 입력을 검증하는 프로그램입니다.

```python
# 나이 검증 프로그램
print("🎂" + "=" * 38 + "🎂")
print("   연령 확인 시스템")
print("🎂" + "=" * 38 + "🎂")

def validate_age(age_str):
    """나이 검증 함수"""
    # 숫자인지 확인
    if not age_str.isdigit():
        raise ValueError("나이는 숫자로 입력해주세요")
  
    age = int(age_str)
  
    # 범위 확인
    if age < 0:
        raise ValueError("나이는 0 이상이어야 합니다")
  
    if age > 150:
        raise ValueError("나이가 너무 많습니다")
  
    return age

def get_age_group(age):
    """연령대 분류"""
    if age < 8:
        return "유아"
    elif age < 14:
        return "어린이"
    elif age < 20:
        return "청소년"
    elif age < 30:
        return "청년"
    elif age < 60:
        return "중년"
    else:
        return "노년"

while True:
    print("\n" + "=" * 40)
    age_input = input("나이 입력 (종료: q): ")
  
    if age_input == "q" or age_input == "Q":
        print("\n프로그램을 종료합니다.")
        break
  
    try:
        age = validate_age(age_input)
        age_group = get_age_group(age)
    
        # 성인 여부
        is_adult = age >= 19
    
        print("\n" + "-" * 40)
        print(f"나이: {age}세")
        print(f"연령대: {age_group}")
        print(f"성인 여부: {'성인' if is_adult else '미성년자'}")
    
        # 영화 관람 등급
        if age >= 19:
            print("영화 관람: 전체 관람 가능")
        elif age >= 15:
            print("영화 관람: 15세 이상 관람가까지")
        elif age >= 12:
            print("영화 관람: 12세 이상 관람가까지")
        else:
            print("영화 관람: 전체 관람가만 가능")
    
        print("-" * 40)
  
    except ValueError as e:
        print(f"\n❌ {e}")
```

---

## 8️⃣ **실전 활용**

### **예제 7: 안전한 ATM**

모든 입력을 검증하는 ATM 프로그램입니다.

```python
# 안전한 ATM 프로그램
print("💳" + "=" * 38 + "💳")
print("   ATM")
print("💳" + "=" * 38 + "💳")

balance = 10000  # 초기 잔액

def get_amount(prompt):
    """금액 입력 받기 (검증 포함)"""
    while True:
        try:
            amount_str = input(prompt)
        
            # 숫자인지 확인
            if not amount_str.isdigit():
                raise ValueError("숫자만 입력해주세요")
        
            amount = int(amount_str)
        
            # 양수인지 확인
            if amount <= 0:
                raise ValueError("0보다 큰 금액을 입력해주세요")
        
            # 단위 확인 (1000원 단위)
            if amount % 1000 != 0:
                raise ValueError("1,000원 단위로 입력해주세요")
        
            return amount
    
        except ValueError as e:
            print(f"❌ {e}")
            print("다시 입력해주세요.\n")

while True:
    print("\n" + "=" * 40)
    print(f"현재 잔액: {balance:,}원")
    print("=" * 40)
    print("1. 입금")
    print("2. 출금")
    print("3. 잔액 조회")
    print("4. 종료")
    print("=" * 40)
  
    choice = input("\n선택: ")
  
    if choice == "1":
        print("\n[ 입금 ]")
    
        try:
            amount = get_amount("입금액: ")
        
            # 입금 한도 확인 (100만원)
            if amount > 1000000:
                raise ValueError("1회 입금 한도는 100만원입니다")
        
            balance = balance + amount
            print(f"\n✓ {amount:,}원 입금 완료")
            print(f"잔액: {balance:,}원")
    
        except ValueError as e:
            print(f"\n❌ {e}")
  
    elif choice == "2":
        print("\n[ 출금 ]")
    
        try:
            amount = get_amount("출금액: ")
        
            # 잔액 확인
            if amount > balance:
                raise ValueError(f"잔액이 부족합니다 (잔액: {balance:,}원)")
        
            # 출금 한도 확인 (50만원)
            if amount > 500000:
                raise ValueError("1회 출금 한도는 50만원입니다")
        
            balance = balance - amount
            print(f"\n✓ {amount:,}원 출금 완료")
            print(f"잔액: {balance:,}원")
    
        except ValueError as e:
            print(f"\n❌ {e}")
  
    elif choice == "3":
        print(f"\n현재 잔액: {balance:,}원")
  
    elif choice == "4":
        print("\n이용해 주셔서 감사합니다.")
        break
  
    else:
        print("\n잘못된 선택입니다.")
```

---

## 📝 **핵심 개념 정리**

### **예외(Exception)**

프로그램 실행 중 발생하는 오류입니다. 처리하지 않으면 프로그램이 중단됩니다.

```python
# 주요 예외 종류
ValueError          # 잘못된 값
ZeroDivisionError   # 0으로 나누기
FileNotFoundError   # 파일 없음
IndexError          # 인덱스 범위 초과
KeyError            # 딕셔너리 키 없음
```

### **try-except 기본**

```python
try:
    # 예외가 발생할 수 있는 코드
    risky_code()
except ValueError:
    # ValueError 처리
    handle_error()
except KeyError as e:
    # 예외 정보 받기
    print(f"오류: {e}")
```

### **완전한 구조**

```python
try:
    code()              # 실행 시도
except ValueError:      # 예외 발생시
    handle_error()
else:                   # 예외 없을 때만
    success()
finally:                # 항상 실행
    cleanup()
```

### **raise - 예외 발생시키기**

```python
def set_age(age):
    if age < 0:
        raise ValueError("나이는 0 이상이어야 합니다")
    return age
```

### **기억할 핵심**

- try-except로 예외를 안전하게 처리
- 구체적인 예외 타입 지정 (ValueError, KeyError 등)
- as로 예외 메시지 받기
- finally는 항상 실행 (정리 작업용)
- raise로 직접 예외 발생 가능

---

## 💡 **실습 과제**

### **과제 1: 안전한 리스트 접근**

인덱스 입력을 받아 리스트 항목을 출력하되, 잘못된 입력을 처리하세요.

```python
# 힌트
fruits = ["사과", "바나나", "오렌지", "포도", "딸기"]

try:
    index = int(input("인덱스: "))
    print(fruits[index])
except ValueError:
    print("숫자를 입력하세요")
except IndexError:
    print("인덱스 범위를 벗어났습니다")
```

### **과제 2: 파일 백업**

파일을 읽어서 백업 파일로 복사하되, 모든 예외를 처리하세요.

```python
# 힌트
try:
    source = input("원본 파일: ")
    with open(source, "r") as f:
        content = f.read()
  
    backup = source + ".backup"
    with open(backup, "w") as f:
        f.write(content)
  
    print(f"백업 완료: {backup}")
  
except FileNotFoundError:
    print("파일을 찾을 수 없습니다")
except PermissionError:
    print("권한이 없습니다")
finally:
    print("작업 종료")
```

---

## ✅ **퀴즈**

### **[초급] 1번**

예외를 처리하는 키워드 조합은?

```python
1. try-catch
2. try-except
3. if-else
4. begin-rescue
```

### **[중급] 2번**

다음 코드의 실행 결과는? (입력: "abc")

```python
try:
    num = int(input("숫자: "))
    print(num * 2)
except ValueError:
    print("오류!")
```

1. 오류!
2. abc
3. abc2
4. 프로그램 중단

### **[중급] 3번**

finally 절의 특징은?

```python
1. 예외 발생시에만 실행
2. 예외 없을 때만 실행
3. 항상 실행
4. 사용 불가
```

### **[고급] 4번**

다음 코드에서 "성공"이 출력되는 경우는?

```python
try:
    x = int(input())
    y = 10 / x
except ZeroDivisionError:
    print("0 오류")
except ValueError:
    print("값 오류")
else:
    print("성공")
```

1. 0 입력
2. abc 입력
3. 5 입력
4. 절대 출력 안 됨

### **[고급] 5번**

예외를 발생시키는 키워드는?

```python
1. throw
2. raise
3. error
4. exception
```

---

## 🔑 **퀴즈 정답 및 해설**

**1번 정답: 2**
파이썬에서는 `try-except` 키워드로 예외를 처리합니다.

**2번 정답: 1**
"abc"는 정수로 변환할 수 없어 `ValueError`가 발생하고, except 블록의 "오류!"가 출력됩니다.

**3번 정답: 3**
`finally` 절은 예외 발생 여부와 관계없이 항상 실행됩니다.

**4번 정답: 3**
5를 입력하면 예외가 발생하지 않아 `else` 블록이 실행되어 "성공"이 출력됩니다. 0이나 abc는 예외가 발생합니다.

**5번 정답: 2**
`raise` 키워드로 예외를 발생시킬 수 있습니다.

---

## 🎯 **다음 장 예고**

다음 장에서는 모듈과 패키지에 대해 배웁니다. 다른 사람이 만든 코드를 재사용하고, 자신의 코드를 모듈로 만들어 관리하는 방법을 학습합니다. 파이썬의 강력한 표준 라이브러리와 외부 패키지 활용법도 배우게 됩니다!

---

수고했습니다.   
조정현 교수([peterchokr@gmail.com](mailto:peterchokr@gmail.com)) 영남이공대학교

이 수업자료는 Claude와 Gemini를 이용하여 제작되었습니다.
